# UX Audit — Synapse v0.6 (July 2026)

Conducted by: Frontend Engineer (senior UX audit role)
Scope: sprint/v0.6 codebase + docs/screens screenshots
Method: code review + screenshot analysis across 10 audit lenses

---

## Executive Summary

Synapse has a coherent visual identity (GitHub-dark-inspired palette, --syn-* token system, Lucide icons) and the structural invariants (virtualisation, no main-thread layout, streaming parse guard) are well respected. The critical friction is concentrated in three areas: (1) the NavRail secondary group uses opaque Italian labels ("Revisione", "Profonda") that are not self-explanatory even in Italian, making the M5 features effectively invisible to new users; (2) the chat panel carries a severe density problem — conversations are all titled "Untitled" with no preview, the message list has no visual separation between turns, and the role labels (YOU / ASSISTANT in bold uppercase green/blue) dominate the content hierarchy; (3) inline-style proliferation across ReviewQueueView, ActivityBar, and Header means dark-mode regressions are one hardcoded hex away. Seven findings are P1 because they produce broken or confusing user flows today.

---

## Findings Table

### P1 — Fix Now (broken or confusing)

| ID | Lens | Evidence | Finding | Recommendation | Effort |
|----|------|----------|---------|----------------|--------|
| UXA-01 | Navigation / IA | NavRail.tsx:82 — IT locale: `nav.deepSearch = "Profonda"`, `nav.lint = "Lint"`, `nav.review = "Revisione"`, `nav.ingest = "Import"` seen as 10px truncated labels in screenshots | The M5 secondary group (below the hairline divider) shows four rail buttons whose labels are not self-describing to a first-time user. "Profonda" (Italian for "Deep") without context looks like a truncation error; "Lint" is a developer term with no tooltip body visible; the divider is a 1px hairline 40px wide — invisible in the screenshots. No section title or group label exists. | Add a visually distinct group label ("Strumenti" / "Tools") above the M5 block; give the divider `border: 1px solid var(--syn-border)` at full rail width; add `title` attribute bodies (tooltip text) that are complete sentences, not just the label repeated. Medium: requires NavRail layout change. | M |
| UXA-02 | Feedback / status | chat-conversation.png: 8 of 11 visible conversations are titled "Untitled" with identical "Jun 28" dates. chat-streaming.png: message body fills entire panel with raw numbers 79–100, no visual turn-boundary or avatar differentiation | Conversation list is useless for navigation when all entries are "Untitled". During streaming the content appears as a raw monospace-adjacent number dump because markdown is deferred — correct per I3, but the streaming experience looks broken to a non-expert eye. | Auto-name conversations from first user message (strip to 40 chars) server-side or locally; add a faint streaming indicator ("Generating…") below the visible token stream so users know the plain-text display is intentional. The conversation list needs a first-message snippet as a secondary line. | M |
| UXA-03 | Interaction / destructive actions | ReviewQueueView.tsx:106–109 — `ITEM_TYPE_COLORS` uses literal `color-mix(in srgb, #1a7f37 10%, white 90%)`. ActivityBar.tsx:627 — `color-mix(in srgb, var(--syn-accent) 8%, transparent)`. ReviewQueueView.tsx:346 — `"color-mix(in srgb, var(--syn-red) 6%, white 94%)"` hardcodes `white` | In dark mode these three sites produce blue/green blobs on a near-black background where the mixed color resolves against white rather than the dark surface — producing light islands that break the dark theme. The `white 94%` item is the generation-error row highlight in ReviewQueueView which is used precisely when a destructive action fails. | Replace all `color-mix(…, white)` with `color-mix(…, var(--syn-mix-base))` (the ADR-0048 pattern already established in theme.css). This is the same fix already applied to theme.css §T1 but not yet carried through to component-level inline styles. | S |
| UXA-04 | Consistency / button styles | MessageList.tsx:315–328, 372–388 — `Save to wiki` and `Regenerate` are styled identically: `background: none; border: 1px solid var(--syn-border); borderRadius: 4; fontSize: 11`. ActionButton in ReviewQueueView.tsx:213–235 uses its own `padding: 3px 10px; fontSize: 11; fontWeight: 600` set, diverging from `syn-button`. Header.tsx server chip dropdown items use yet another raw button style. 4+ divergent button micro-patterns exist outside the `.syn-button` class. | None of these "secondary ghost button" patterns use the `.syn-button syn-button--secondary` token class defined in theme.css:266–270, meaning they won't inherit theme transitions, disabled states, or future design-system changes. | Refactor `Save to wiki`, `Regenerate`, and `ActionButton` to use `.syn-button syn-button--secondary` or a new `.syn-button--ghost` variant added to theme.css. Reduces component-level inline style count significantly. | M |
| UXA-05 | Accessibility / focus | NavRail.tsx:274 — `outline: isActive ? "1px solid color-mix(…)" : "none"` suppresses the native focus ring on inactive buttons. theme.css:200–207 provides a global `button:focus-visible` ring, but the inline `outline: "none"` on inactive rail buttons overrides it for keyboard nav. | A keyboard user tabbing through the rail loses the focus indicator on all items that are not currently active. WCAG 2.4.7 requires visible focus. | Remove `outline: "none"` from inactive rail button inline style. The `:focus-visible` rule in theme.css will then supply the correct ring automatically. For active buttons, the `outline: 1px solid color-mix(…)` used as a persistent indicator should be renamed to a class or renamed CSS property so it does not collide with keyboard focus. | S |
| UXA-06 | First-run / empty vault | ingest-section.png + ingest-upload.png: 8 completed runs all show "0 pages created" and "Cost: $0.0000". The right panel shows "Select a run to see details." but clicking on any of these zero-result runs presumably shows nothing meaningful. review-queue screenshots are populated so ingest ran but produced nothing — no explanation is shown to the user as to why 0 pages were created. | A new user who drops a file, triggers ingest, sees "Completed / 0 pages created / $0.00" has no signal about what went wrong or what to do next. This is the most common new-user failure mode. | Add a contextual hint row in the IngestRunDetail panel when `pages_created === 0 && status === "completed"`: e.g. "No pages were extracted. Check that your file contains substantive text and a provider is configured." Link to Settings. | S |
| UXA-07 | Graph / interaction | graph-obsidian-node-selected.png: when a node is selected, the panel at right shows internal UUID (`b9e96561-2ef4-484b-839a-...`), raw `Position -9.27, 3.23`, and `Degree 1` — all in a monospace-ish font with no visual grouping. shell-3panel-selected.png confirms: the ID field shows a truncated UUID that is meaningless to end users. | Exposing raw internal UUIDs and floating-point graph coordinates as primary metadata fields degrades the professional quality of the detail panel and gives users no actionable information. | Hide the UUID entirely or move it to a collapsed "Technical" sub-section. Replace raw coordinate display with a human label ("Position in graph" or remove). Promote "Degree" as "Connected to N other pages" in plain language. | S |

### P2 — Friction

| ID | Lens | Evidence | Finding | Recommendation | Effort |
|----|------|----------|---------|----------------|--------|
| UXA-08 | Visual hierarchy / typography | MessageList.tsx:506–528 — role labels are `fontSize: 11, fontWeight: 600, textTransform: uppercase, letterSpacing: 0.05em` in bright `var(--syn-accent)` (blue) for YOU and `var(--syn-green)` for ASSISTANT. chat-conversation.png: these labels are visually heavier than the message text itself. | The role identifiers dominate the hierarchy over the actual content — the opposite of what a chat interface should do. In a thread, the role alternation is already implied by position; the label should be secondary. | Reduce role labels to 9px, `var(--syn-text-dim)`, no uppercase (or keep uppercase but at 8px). Use a left-border stripe (3px solid, `var(--syn-accent-soft)` / `var(--syn-notice-success-bg)`) as the primary visual differentiator between turns, removing the dependency on color alone (also improves CVD accessibility). | S |
| UXA-09 | Layout / spacing | shell-3panel.png and shell-3panel-selected.png: the tree panel shows "QUICK START" section and then "CONCEPTS [88] ▾" with item nodes that use a 6px coloured dot + text in small font. The right preview panel at 28% viewport width shows "Select a node in the graph or a page in the tree to see details." in centred muted text. When selected (shell-3panel-selected.png), the panel immediately shows UUID metadata. No skeleton state exists between "nothing selected" and "loaded". | The preview panel empty state ("Select a node…") disappears instantly and is replaced with raw metadata; there is no loading skeleton, no transition, and the content jump is jarring. | Add a 200ms CSS fade transition on panel content swap; add a skeleton placeholder (3 grey bars) during the data fetch after node selection. | S |
| UXA-10 | Consistency / modal patterns | ConfirmDialog.tsx is a well-structured `alertdialog` with focus trap and Esc key. However, ReviewQueueView.tsx does not use a modal for destructive per-item "Dismiss" action — it fires immediately on click with no confirmation. The bulk "Dismiss" in the selection bar also fires immediately. "Delete" in SourcesView does use ConfirmDialog (via bulk.deleteDialogTitle key in IT locale). | Three destructive patterns exist: (a) modal confirm (cascade delete, cancel-all queue), (b) immediate action (review dismiss, review skip), (c) per-item undo not provided. Skip and Dismiss in the review queue are permanent state transitions with no recovery UI. | At minimum, Dismiss should show a ConfirmDialog (it is irreversible). Skip is more recoverable (item stays in DB) so immediate is acceptable but should have an inline undo toast within 5 seconds. | M |
| UXA-11 | Feedback / activity bar | ActivityBar.tsx:741–757 — 28px footer bar shows: vault emoji (folder icon) + vault-id + "v" prefix + data version + uptime arrow + connectivity dot + status text + provider. At 11px in a 28px bar with 7 simultaneous data points, the bar is information-dense to the point of illegibility on a 1080p display. The vault emoji (&#128193;) is a browser emoji not a Lucide icon. | The bottom bar mixes operational status (running tasks, version) with permanent metadata (vault id, uptime) in one visual plane. The folder emoji is inconsistent with the rest of the Lucide icon set. | Introduce two visual tiers within the 28px bar: permanent metadata (vault id, version) on the left at subdued opacity; operational status (queue, provider) on the right at full opacity. Replace the folder emoji with `<FolderOpen size={11} />` from the existing Lucide import. | S |
| UXA-12 | Interaction / loading states | provider-selector-open.png: the dropdown shows all configured providers including multiple duplicate "Local (Ollama) / qwen2.5:3b" entries (two Per-operation rows, one Vault row). There is no deduplication, no explanation of why the same model appears three times, and no hint about which one "wins" for the current vault. settings-llm-models.png confirms 6 rows including two identical "Local (Ollama) / qwen2.5:3b Per-operation" entries. | Duplicate provider entries create confusion about which provider is actually active. The "Per-operation" scope label (from `provider.scope.operation` = "Per-operation" in EN) is not explained anywhere in the selector UI; users cannot infer which entry takes precedence. | Add deduplication in the selector (same type+model_id+scope = show once, fold duplicates under a "×2" count chip). Add a single-line tooltip or sub-label explaining scope precedence: "Vault overrides Global; Per-operation is resolved by the backend per-call." | M |
| UXA-13 | Settings / IA | settings-import-schedule.png (old single-panel Settings view) vs settings-section.png (new two-column Settings with sidebar nav): two visually inconsistent Settings layouts exist in the screenshots. The old view shows "CONTEXT WINDOW", "LANGUAGE", "PROVIDER CONFIGURATION", "SCHEDULED FOLDER IMPORT" in a single scroll. The new view (settings-section.png) shows a sidebar nav and a content panel with correct two-column structure. | The inconsistency between screenshots suggests either the old layout is from a stale screenshot or the old SettingsPanel code path still exists at some route. Budget split values also differ: settings-import-schedule.png shows `19.2001953125K` (floating-point precision error) while settings-section.png shows `19.2K`. | Verify the old Settings layout is dead code and remove it. Fix the budget display to use `Math.round(n / 1000 * 10) / 10 + "K"` formatting (the `19.2001953125K` value is a raw float-to-string issue). | S |
| UXA-14 | Dark mode / hardcoded colors | Header.tsx:178 — server chip green dot: `background: "#22c55e"` (hardcoded hex, not a token). Header.tsx:246 — current-server dot: same `#22c55e`. ConnectScreen.tsx:283 — error color `#ef4444` is not `var(--syn-red)`. ConnectScreen.tsx:308 — disabled button uses `var(--syn-border)` for background but the gradient uses literal `#2563eb` and `#5b4de0`. | ConnectScreen is the first screen a desktop user sees. Hardcoded colors in it mean dark mode ConnectScreen would have a mismatched gradient and hardcoded red error text that may not meet contrast requirements on a dark background. | Replace `#22c55e` → `var(--syn-green)`, `#ef4444` → `var(--syn-red)`, and the gradient in the disabled state → `var(--syn-border)` (already done for disabled background, but the active gradient should use `var(--syn-accent)` as the start color). | S |
| UXA-15 | Accessibility / ARIA | ProviderSelector.tsx:185 — the dropdown panel uses `role="dialog"` but has no `aria-modal="true"` and the listbox inside uses `role="listbox"` with `role="option"` buttons — mixing dialog and listbox roles is semantically incorrect. The trigger button has `aria-haspopup="listbox"` which conflicts with the actual `role="dialog"` on the panel. | Screen readers will announce the provider selector as a dialog but the interaction model is a listbox, producing confusing announcements. | Choose one pattern: either a proper `role="listbox"` disclosure (no dialog wrapper, trigger with `aria-haspopup="listbox"`, options with `role="option"`) or a `role="dialog"` with a contained listbox. Given the scope toggle, a dialog wrapping a listbox is more appropriate — keep `role="dialog" aria-modal="true"` on the panel and remove `role="listbox"` from the inner list container; buttons remain as standard buttons. | M |
| UXA-16 | Interaction / toast | Toast.tsx:93 — close button uses Unicode `✕` (U+2715) as its text node. Toast role is `role="status"` (Toast.tsx:80) but an error toast should be `role="alert"` (live region with `assertive` politeness). The `aria-live="polite"` on the container applies to all toasts uniformly. | Error toasts with `role="status"` are announced with polite priority, meaning screen readers may delay announcing them if the user is busy reading other content. A failed ingest or save error may never be announced. | Set `role={isError ? "alert" : "status"}` on individual ToastItem. Replace `✕` text node with `<X size={12} />` from the Lucide import already used elsewhere in the file's sibling components. | S |
| UXA-17 | Navigation / IA | it.json:9 — `nav.deepSearch = "Profonda"` (10 chars, renders at 10px, max-width truncated). en.json:13 — `nav.deepSearch = "Research"` (8 chars, fine). The Italian label for the "Deep Search" section is not "Ricerca Profonda" (the full term used everywhere else in the IT locale, e.g. `review.deepResearch = "Ricerca Profonda"`) but just "Profonda" — an adjective without its noun, unreadable in isolation. | An Italian user sees the nav rail label "Profonda" with no noun context. The matching full-form "Ricerca Profonda" is used in the Review Queue and Research panels but not in the nav label. This inconsistency also prevents the label from being discoverable by Italian speakers searching for "research" functionality. | Change `nav.deepSearch` in it.json from `"Profonda"` to `"Ricerca"` (fits in 10px) or use the existing `nav.deepSearchShort = "Ricerca"` key (already defined in it.json:13) as the label source. The `nav.deepSearch` key should be renamed the short form; reserve the long form for aria-labels and page headings. | S |

### P3 — Polish

| ID | Lens | Evidence | Finding | Recommendation | Effort |
|----|------|----------|---------|----------------|--------|
| UXA-18 | Visual hierarchy | review-queue-adr0034.png: first visible card shows `review.itemType.new_page` as a raw i18n key ("review.itemType.new_page") rendered as the badge label — a missing translation key. The EN locale has `review.itemType.missing-page` (hyphenated) but the component passes `item_type` which may be `"new_page"` (underscored) from the backend, resulting in a key-miss fallback. | Missing translations render as raw dotted key strings, breaking the professional quality of the Review panel. | Normalise item_type values to kebab-case in the API client layer (`new_page` → `missing-page`) OR add a normalisation step in `ItemTypeBadge` before the `t()` call. Add a unit test that asserts all known item_type strings map to valid translation keys. | S |
| UXA-19 | Graph interaction | graph-obsidian-node-selected.png: when a node is selected via hover, the non-neighbour nodes are visually dimmed (greyed circles) but labels for dimmed nodes remain fully opaque and crowd the centre cluster. At the selected state, 30+ overlapping labels compete for attention. | The hover-neighbour dimming works correctly for node circles but does not extend to node labels, creating label clutter that makes the selected node's relationships harder to read. | Reduce label opacity of non-neighbour nodes to 0.15 during hover selection (sigma.js node/edge program layer; already within GraphViewer scope). | M |
| UXA-20 | Consistency | settings-llm-models.png: "Delete" buttons on each provider row are styled with `background: var(--syn-red)` (red fill, white text — a P1-style destructive button). The rest of the Settings UI uses ghost/outline buttons. The Delete button on the LLM Models page is the only filled-red button in the entire application outside of ConfirmDialog. | Mixed destructive button convention: filled-red in LLM Models, outline-red in ReviewQueueView ("Clear resolved"), no-color immediate in ReviewQueue dismiss. | Standardise destructive actions: use outline-red (`border: 1px solid var(--syn-red); color: var(--syn-red); background: transparent`) for non-modal destructive buttons; filled-red only inside ConfirmDialog confirm button. This matches the existing pattern in ConfirmDialog.tsx:177–186. | S |
| UXA-21 | Feedback / copy quality | ActivityBar.tsx:733 — hardcoded English string: `"+${counts.failed - MAX_VISIBLE_FAILED} more failed tasks"` bypasses i18n entirely. | This string will appear in Italian without localisation. | Add an i18n key `activity.moreFailedTasks` and use `t("activity.moreFailedTasks", { count: counts.failed - MAX_VISIBLE_FAILED })`. | S |
| UXA-22 | Settings / i18n naturalness | it.json — several i18n strings use formal/bureaucratic Italian where informal would be clearer: `"Impossibile raggiungere il server. Controlla l'indirizzo e riprova."` (connect.errors.unreachable) is acceptable but `"Il backend è in esecuzione?"` (used as a standalone question appended to 6 different error messages) reads oddly as a fragment rather than a complete actionable error. | Repeating `"Il backend è in esecuzione?"` as a suffixed fragment across Embeddings, MCP, Web Search, Web Clipper, and Maintenance error messages makes errors feel copy-pasted and does not guide the user toward a solution. | Restructure the repeated pattern into a shared base + specific action: e.g. `"Impossibile caricare {{section}}. Assicurati che il backend sia in esecuzione e riprova."` with a single `settings.backendDownHint` key. | S |
| UXA-23 | First-run | ConnectScreen.tsx:263–274 — when a local backend is auto-detected, the `connect.detected` text shows `"Rilevato un server Synapse su questa macchina"` but there is no visual indicator (colour, icon, checkmark) distinguishing the detected-state from the normal state. Only the text changes. | Without a visual cue (green dot, checkmark icon), the auto-detect message is easy to miss, especially since the URL input already shows `http://localhost:8000` as a pre-filled value. | Add `<CheckCircle2 size={13} color="var(--syn-green)" />` inline before the detected text. This mirrors the pattern used in the ActivityBar and ProviderSelector for confirmed states. | S |
| UXA-24 | Density / spacing | review-queue screenshots: the proposal cards have 3px gap between rows (ReviewQueueView.tsx:341 `gap: 3`), which at 11–12px font size means text rows are almost touching. The card has no visual separation from adjacent cards beyond a 1px `var(--syn-border)` rule. The card background is `var(--syn-bg)` (same as page background), so adjacent cards blend together. | Without card-level visual separation, the Review Queue reads as a flat text dump rather than a list of actionable proposals. On a 16:9 monitor with 27+ items, the visual density induces scanning fatigue. | Increase row gap to 6px within cards; alternate card backgrounds using `var(--syn-bg)` / `var(--syn-bg-soft)` (zebra rows), or add `padding: 12px 16px` with `border-radius: var(--syn-radius-sm)` and `margin-bottom: 4px` per card on a `var(--syn-bg-soft)` page background. | S |
| UXA-25 | Desktop specifics | Header.tsx:184 — the dropdown chevron is a Unicode triangle `"▲"` / `"▼"` rendered at `fontSize: 9`. In the server chip screenshots the chevron is barely visible. The ConnectScreen.tsx gradient bar (top accent stripe) uses `linear-gradient(90deg, #2563eb 0%, #8250df 100%)` — hardcoded hex outside of theme token coverage. | Inconsistent use of decorative elements: Unicode triangles vs Lucide `<ChevronDown>` used everywhere else in the codebase (ActivityBar, NavRail). The gradient is also not token-aware for dark mode. | Replace `▲`/`▼` with `<ChevronUp size={9} />` / `<ChevronDown size={9} />`. For the ConnectScreen gradient, either accept it as a branding exception (document in a comment) or create `--syn-brand-gradient` token used in both ConnectScreen and the NavRail Logo. | S |
| UXA-26 | Accessibility / keyboard | CommandPalette — the `⌘K` shortcut is registered (AppShell.tsx:54 `useGlobalShortcuts`) and the palette component exists (common/CommandPalette.tsx). However, in the screenshots the palette is never visible, and there is no visible keyboard shortcut indicator anywhere in the UI — not in the header, not in the nav rail tooltips, not in an empty-state hint. | Users who do not read the source code cannot discover `⌘K`. The palette is the fastest navigation path but is effectively hidden. | Add `⌘K` / `Ctrl+K` hint to the NavRail header area (below the Synapse logo, in `var(--syn-text-dim)` 9px) or to a Settings → Interface page. | S |
| UXA-27 | Graph / empty state | navrail-graph-active.png (collapsed panels): the graph fills the viewport without side panels. The legend (NODE TYPES) floats bottom-left. There are no zoom controls visible in screenshots — the graph toolbar may be hidden behind collapsed panels or off-screen. | Without visible zoom-in/zoom-out/fit buttons, the graph is keyboard-inaccessible for zoom operations and mouse-only users must rely on scroll wheel, which has no affordance. | Verify graph toolbar (`graph-toolbar` CSS class referenced in theme.css:514) is always visible regardless of panel state. If it overlaps the collapsed-panel toggle, raise its z-index. Add visible `+` / `-` / `⊡` buttons. | M |
| UXA-28 | Interaction / animation | ActivityBar.tsx:882–889 — `@keyframes spin` and `@keyframes taskBarSweep` are injected as an inline `<style>` tag inside the ActivityBar component body. This means the keyframes are injected every time ActivityBar mounts. While React deduplicates DOM nodes, the `<style>` injection is an antipattern that can cause issues with SSR or multiple ActivityBar mounts in tests. ReviewQueueView.tsx:891 injects `@keyframes syn-spin` independently, duplicating the spin animation. | Two copies of a spin keyframe exist in component-level `<style>` tags. This is a design-system hygiene issue — keyframes should live in a global CSS file. | Move both `@keyframes spin` / `syn-spin` / `taskBarSweep` to `theme.css` (already the global CSS entry point) and remove the inline `<style>` tags from both components. | S |

---

## Quick Wins (S-effort, P1 or P2)

These 10 items can each be resolved in under 2 hours of focused work and collectively remove the most visible friction:

1. **UXA-03** — Replace `color-mix(…, white)` with `color-mix(…, var(--syn-mix-base))` in ReviewQueueView.tsx (3 sites). Dark mode regression fix.
2. **UXA-05** — Remove `outline: "none"` from inactive NavRail button inline style (1 line deletion). Restores keyboard focus visibility.
3. **UXA-06** — Add a contextual hint in IngestRunDetail when `pages_created === 0 && status === "completed"`. Critical new-user signal.
4. **UXA-07** — Hide UUID and raw coordinate fields from the graph node detail panel; show "Connected to N pages" instead of raw Degree. 2–3 line change.
5. **UXA-14** — Replace `#22c55e` → `var(--syn-green)` and `#ef4444` → `var(--syn-red)` in Header.tsx and ConnectScreen.tsx. Token hygiene.
6. **UXA-16** — Set `role={isError ? "alert" : "status"}` on ToastItem. Screen reader correctness, 1 line.
7. **UXA-17** — Change `nav.deepSearch` in it.json from `"Profonda"` to `"Ricerca"`. 1-character file edit.
8. **UXA-18** — Add `item_type` normalisation in ItemTypeBadge (replace `_` with `-` before `t()` call). Fixes raw key display in Review Queue.
9. **UXA-21** — Replace hardcoded English `"more failed tasks"` string in ActivityBar.tsx with `t()` call. i18n hygiene.
10. **UXA-23** — Add `<CheckCircle2>` icon before the auto-detected server hint in ConnectScreen.tsx. First-run polish.

---

## Design-System Debt Note

The current approach uses `theme.css` for token definitions and `.syn-*` utility classes, but component files (ReviewQueueView, ActivityBar, Header, MessageList, ConnectScreen) contain hundreds of inline style objects that duplicate token-derived values, bypass class-based theming, and make global changes require hunting through TSX files.

Recommended strategy for v0.9:

**Do not introduce a CSS-in-JS library.** Instead:

1. Extend `theme.css` with a `components.css` layer containing scoped class rules for recurring patterns: `.syn-button--ghost`, `.syn-meta-row`, `.syn-card-row`, `.syn-notice-banner`, `.syn-role-label`. These already exist conceptually but are expressed as repeated inline objects.
2. Define a strict rule: **no inline style may reference a raw hex color or a raw pixel value that is not derived from a `--syn-*` token.** Raw pixel values for layout (widths, flex gaps) are acceptable inline; color values never are.
3. Audit priority order: ReviewQueueView.tsx (highest inline-style count, dark mode risk), ActivityBar.tsx (keyframes, status bar), Header.tsx (server chip), ConnectScreen.tsx (branding gradient).
4. The `--syn-bg-card` token referenced in ActivityBar.tsx:544 and ConfirmDialog.tsx:118 does not exist in theme.css — it falls back to `var(--syn-bg-soft)` silently. Add it explicitly to theme.css or remove the undefined reference.

Estimated remediation: M5-level effort to bring inline-style-to-class ratio from ~70/30 to ~20/80 across the affected components.

---

## Remediation Status — v0.9.0 (2026-07-03)

The following audit items shipped in the v0.9.0 «Trust & observability» release.

### W0 — All P1 findings resolved

All seven P1 findings (UXA-01 through UXA-07) were addressed in v0.9. Summary:

| ID | Finding | Resolution |
|----|---------|-----------|
| UXA-01 | Nav rail secondary group opaque Italian labels | Added group label "Strumenti / Tools" above M5 block; divider uses `var(--syn-border)` at full width; `title` attributes use complete-sentence tooltip text. |
| UXA-02 | Conversation list all "Untitled"; streaming looks broken | Auto-title from first message (50-char trim); conversation list shows one-line message preview (UXB-1, R9-7). Streaming indicator ("Generando…") shown below the live token stream. |
| UXA-03 | `color-mix(…, white)` dark-mode regression | All three `color-mix(…, white 94%)` sites in ReviewQueueView.tsx replaced with `color-mix(…, var(--syn-mix-base))`. Dark mode confirmed. |
| UXA-04 | 4+ divergent ghost-button micro-patterns | `components.css` introduced with `.syn-button--ghost` and `.syn-button--secondary` classes. `Save to wiki`, `Regenerate`, and `ActionButton` migrated. |
| UXA-05 | Keyboard focus ring suppressed on inactive NavRail buttons | `outline: "none"` removed from inactive rail button inline style; `:focus-visible` ring from theme.css now applies correctly. |
| UXA-06 | 0-pages-created ingest run shows no guidance | Contextual hint row added to `IngestRunDetail` when `pages_created === 0 && status === "completed"`. Links to Settings. |
| UXA-07 | Raw UUID and float coordinates exposed in graph node panel | UUID hidden. Coordinates removed. Degree relabeled as "Connected to N other pages". |

### UXB items resolved

| ID | Finding | Resolution |
|----|---------|-----------|
| UXB-1 | Conversation density / all "Untitled" | Auto-title + message preview shipped (R9-7, 2026-07-03). |
| UXB-2 | Nav rail label "Profonda" (UXA-17) | `nav.deepSearch` in `it.json` changed from `"Profonda"` to `"Ricerca"`. |

### Remaining open items (P2 / P3 — not targeted in v0.9)

The following findings were identified in the audit but were not included in v0.9 scope.
They remain open for future sprints:

| ID | Priority | Finding (short) | Target |
|----|----------|----------------|--------|
| UXA-08 | P2 | Role labels (YOU/ASSISTANT) visually dominate message content | v1.0 or polish sprint |
| UXA-09 | P2 | No skeleton state between empty and loaded panel | future |
| UXA-10 | P2 | Dismiss in Review fires immediately with no confirmation | future |
| UXA-11 | P2 | Status bar 7-point density; folder emoji inconsistency | future |
| UXA-12 | P2 | Duplicate provider entries in selector; no precedence hint | future |
| UXA-13 | P2 | Old Settings layout screenshot stale; float precision bug | future |
| UXA-14 | P2 | Hardcoded hex colors in Header/ConnectScreen | future |
| UXA-15 | P2 | Mixed `role="dialog"` / `role="listbox"` in ProviderSelector | future |
| UXA-16 | P2 | Error toast should use `role="alert"` not `role="status"` | future |
| UXA-18 | P3 | Raw i18n key in Review Queue item type badge | future |
| UXA-19 | P3 | Non-neighbour node labels not dimmed during hover | future |
| UXA-20 | P3 | Mixed destructive button convention (filled-red vs outline-red) | future |
| UXA-21 | P3 | Hardcoded English "more failed tasks" string in ActivityBar | future |
| UXA-22 | P3 | Bureaucratic Italian error fragments | future |
| UXA-23 | P3 | No visual cue for auto-detected server in ConnectScreen | future |
| UXA-24 | P3 | Review queue card density (3px gap) | future |
| UXA-25 | P3 | Unicode chevrons in Header; ConnectScreen hardcoded gradient | future |
| UXA-26 | P3 | ⌘K shortcut not discoverable in UI | future |
| UXA-27 | P3 | Graph zoom controls may be hidden behind collapsed panels | future |
| UXA-28 | P3 | Inline `<style>` keyframe injection in ActivityBar / ReviewQueueView | future |
