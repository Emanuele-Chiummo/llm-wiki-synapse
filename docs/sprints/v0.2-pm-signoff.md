# Sprint 2 — v0.2 (M2) — PM Exit-Criteria Sign-Off

> Author: product-manager
> Date: 2026-06-28
> Milestone: M2 — "Agentic loop closed, 3 providers"
> Branch: sprint/v0.2
> Prerequisite closed: M1 (docs/sprints/v0.1-m1-closure.md)
> Feature IDs in scope: F17, F3, K2 (ingest op), K3, K5, MCP server, D3, D7, D1/D2/D4 updates
> Invariants with heightened priority: I6 (pluggable inference), I7 (bounded loops)

---

## 1. Gate sign-off register

| Gate | Owner | Verdict | Source document |
|------|-------|---------|-----------------|
| QA green | qa-test-engineer | MET-PENDING-LIVE-SMOKE | docs/sprints/v0.2-qa-report.md — 299 passed / 3 live-skipped / 0 failed; BUG-v0.2-1 found and fixed; ruff + black + mypy clean |
| Architect review | solution-architect | APPROVED | docs/sprints/v0.2-architect-review.md — I6/I7/§12 all APPROVE; all 5 D-artifact categories approved; 0 blocking defects; 3 non-blocking follow-ups |
| Tech-writer docs gate | tech-writer | UP-TO-DATE | DOCS_STATUS.md — all v0.2 D-artifacts (D1/D2/D3/D4/D7) present, consistent, zero drift; cross-consistency sweep clean; 2 non-blocking cosmetic items |
| PM exit-criteria sign-off | product-manager | THIS DOCUMENT | |
| Human checkpoint | Emanuele | PENDING — deferred to live TrueNAS run | Conditions listed in §6 |

All 4 automated/agent gates are positive. The human checkpoint (EC-M2-17) is pending live
execution, consistent with the v0.1 precedent (M1 human checkpoint was similarly deferred and
subsequently confirmed on the MacBook Air M2 live run).

---

## 2. EC-M2-1..17 — exit criteria walk

| EC | Feature IDs | Description | Verdict | Evidence |
|----|-------------|-------------|---------|----------|
| EC-M2-1 | F17 | ABC + 3 backends complete; chat() stub; static I6 guard | MET | AC-F17-1..8 all GREEN. ABC with 4 typed methods in base.py; all 3 backends implement it; chat() raises NotImplementedError (T-SCH-001..020). Static guard: T-CQ-001..010 — zero direct Ollama/Anthropic/SDK imports outside provider/; zero hardcoded model IDs in app/. I6 APPROVED by architect. |
| EC-M2-2 | F17 | provider_config table + REST endpoints + resolution order | MET | AC-F17-5..7 GREEN. provider_config Alembic migration 0002 verified (test_models_schema T-PG-020..025c); GET/POST/DELETE /provider/config in OpenAPI (DOCS_STATUS zero drift); operation > vault > global resolution order verified by unit test (T-PCFG-*). |
| EC-M2-3 | F17, K2 | Capability routing reads supports_agentic_loop only — no isinstance/class check | MET | AC-K2-4, AC-F17-3 GREEN. Architect review §1 confirms routing is single-signal (orchestrator.py:313/322). Custom fake with supports_agentic_loop=True delegates without being a CliAgentProvider instance (T-ROUTE-001..015). |
| EC-M2-4 | K2, I7 | Bounded loop stops at max_iter=3; no overrun; total_cost_usd logged | MET | AC-K2-5..7 GREEN. T-LOOP-001..020 cover: non-converging mock halts at exactly max_iter=3 with converged=False; token_budget halts before unaffordable call; fallback bounded to one retry; ingest_runs row written with total_cost_usd on every path. I7 APPROVED by architect. |
| EC-M2-5 | F3, F17, K2, MCP | 3-provider smoke matrix on live TrueNAS | MET-PENDING-LIVE-SMOKE | CI mock tests GREEN (SYNAPSE_SMOKE_MOCK=1): T-SMOKE-LOCAL-MOCK, T-SMOKE-API-MOCK, T-SMOKE-CLI-MOCK. Live harness ready: backend/scripts/smoke_providers.py + backend/tests/test_smoke_providers.py (marked live). Fixture: backend/tests/fixtures/sample-source.md. Run commands listed in §6. |
| EC-M2-6 | F3, K2, K3 | Generated pages schema-valid; frontmatter complete; index.md + overview.md updated | MET-PENDING-LIVE-SMOKE | Schema validation automated: AC-F3-1..3 + AC-F3-5..7 GREEN via test_schemas.py and mock smoke tests. AC-F3-4 (language detection EN/IT live) LIVE-DEFERRED. Real-content assertion (vault/wiki/ pages from actual LLM) verified during EC-M2-5 live run. |
| EC-M2-7 | MCP server | 4 MCP tools registered; integration test green; mcp-tools.json present | MET | AC-MCP-1..7 GREEN. T-MCP-001..015 cover all 4 tools via FastMCP in-process client; write_page routes through shared write_wiki_page seam (ADR-0010); mcp-tools.json present and verified by T-DOCS-028..028e. Architect confirms single write path (K5/K3/Qdrant all run identically for MCP and orchestrated paths). |
| EC-M2-8 | K5 | Wikilink parser; links table; dangling stored + warned | MET | AC-K5-1..7 all GREEN. T-WL-001..020: plain link, aliased link, section link, multiple, zero, invalid brackets — no crash. links table columns match spec (T-PG-030..030d). Dangling resolution: warn-not-error confirmed by architect review §5. |
| EC-M2-9 | K3 | index.md auto-updated after each page write; valid frontmatter; idempotent | MET | AC-K3-1..6 all GREEN. T-IDX-001..015 cover: wikilink entry per generated page, frontmatter preserved, type grouping, recreation if absent, idempotent (byte-stable for same DB state confirmed by architect). update_index() queries DB state — does not rescan vault filesystem (I1). |
| EC-M2-10 | D3 | Both sequence diagrams present, content-checked, manually reviewed | MET | AC-D3-1..2 GREEN (T-DOCS-026..027c — file exists, sequenceDiagram keyword, both branches / loop bounds present). AC-D3-3 (mmdc CI render) deferred to v0.3 devops task — consistent with v0.1 carry-forward pattern. AC-D3-4 MANUAL: tech-writer signed off (DOCS_STATUS §4); architect approved diagram accuracy (v0.2-architect-review §6). |
| EC-M2-11 | D7 | ADR-0007/0008/0009 + additional ADRs present; architect-reviewed | MET | AC-D7-1..4 GREEN (T-DOCS-029..029c). All 5 v0.2 ADRs (0007–0011) present, non-empty, reference I6/I7. ADR README indexes all 11. Architect signed off all 5 (v0.2-architect-review §6). Tech-writer confirmed internally consistent (DOCS_STATUS §6). |
| EC-M2-12 | D1, D2, D4 | C4 component.mmd added; ER updated for 5 tables; OpenAPI typed 202 + provider endpoints; mcp-tools.json | MET | AC-D1u-1..2, AC-D2u-1, AC-D4u-1..2 all GREEN. component.mmd new (T-DOCS-023..025); ER 5 tables zero drift (DOCS_STATUS §2); openapi.json zero drift including typed 202 IngestTriggerResponse and GET/POST/DELETE /provider/config (DOCS_STATUS §3); mcp-tools.json 4 tools (T-DOCS-028..028e). v0.1 carry-forward (202 schema) resolved. |
| EC-M2-13 | I5, K7 | vault/wiki/ opens in Obsidian after real LLM-generated pages | MET-PENDING-LIVE | Automated I5 checks GREEN: test_obsidian_check.py (15 tests) covers .obsidian config, YAML frontmatter enforcement, wikilink write-readback. Manual open-in-Obsidian with real pages from EC-M2-5 smoke run is the remaining human step. |
| EC-M2-14 | QA gate | Full suite green; no P0/P1 bugs; I7 loop test + I6 static test pass | MET | 299 passed / 3 live-skipped / 0 failed. BUG-v0.2-1 (delegate_ingest bool→tuple[bool,int]) found and fixed during QA, re-tested green. mypy-1 (4 strict errors in non-critical files) non-blocking, filed to backend-engineer. QA signed off MET-PENDING-LIVE-SMOKE. |
| EC-M2-15 | Architect gate | Architect reviewed base.py, orchestrator.py, provider_config schema, MCP server, K5 links table; ADRs approved | MET | docs/sprints/v0.2-architect-review.md — APPROVED with 0 blocking defects. All 5 merge conditions from v0.2-architecture §9 satisfied. Diagrams (D1/D2/D3/D7) signed off. |
| EC-M2-16 | Tech-writer gate | D3 + D7 + D1/D2/D4 verified consistent; no stale diagrams | MET | DOCS_STATUS.md — DOCS GATE UP-TO-DATE. All required D-artifacts pass. Cross-consistency sweep clean (no structural contradictions). 2 non-blocking cosmetic items (openapi.json version string; mmdc CI step) do not hold the gate. Signed: tech-writer 2026-06-28. |
| EC-M2-17 | Human checkpoint | Emanuele has observed live smoke matrix + confirmed Obsidian vault | PENDING — deferred to live TrueNAS run | Conditions and commands listed in §6 |

### EC-M2 roll-up

| Category | Count | ECs |
|----------|-------|-----|
| MET — fully automated/verified now | 13 | EC-M2-1, 2, 3, 4, 7, 8, 9, 10, 11, 12, 14, 15, 16 |
| MET-PENDING-LIVE-SMOKE — mock green, live run required | 2 | EC-M2-5, 6 |
| MET-PENDING-LIVE — automated partial, manual step remaining | 1 | EC-M2-13 |
| PENDING — human checkpoint | 1 | EC-M2-17 |
| NOT MET | 0 | — |

Total: 13 MET + 3 pending-live + 1 pending-human = 17/17 tracked. Zero NOT MET.

---

## 3. Anti-scope-creep check

### In-scope feature IDs confirmed delivered

All in-scope IDs from docs/sprints/v0.2-scope.md §2 are delivered:

| Feature ID | Delivered |
|------------|-----------|
| F17 — InferenceProvider ABC + 3 backends + provider_config + REST endpoints | YES |
| F3 — two-step CoT ingest, source traceability, overview.md, language-aware | YES |
| K2 (ingest op) — orchestrator with capability routing + bounded loop | YES |
| K3 — index.md auto-maintained | YES |
| K5 — wikilink parser + links table | YES |
| MCP server — FastMCP 4-tool standalone server | YES |
| D3 — both sequence diagrams (new) | YES |
| D7 — ADR-0007/0008/0009 + additional 0010/0011 (new) | YES |
| D1 — component.mmd (new) + context/container updated | YES |
| D2 — ER updated for 5 tables via make er | YES |
| D4 — openapi.json updated + mcp-tools.json (new) | YES |

### Out-of-scope check — CLEAN

The following items are CONFIRMED NOT introduced in v0.2 (per architect review, QA static checks, and scope lock):

- F4 (knowledge graph): no FA2, no igraph, no sigma.js. links table is data-only (graph edge source; I2 not yet exercised). CLEAN.
- F1 (3-panel UI): no frontend code in v0.2. CLEAN.
- F6 (multi-conversation chat): chat() is a stub raising NotImplementedError on all 3 backends; confirmed by T-SCH-001..020 and architect review §1. No chat endpoint, no conversation persistence. CLEAN.
- F7 / F8 / F14: no streaming display, no LaTeX conversion, no context-window UI. CLEAN.
- F10 (deep research): MCP search_wiki is local wiki search (Qdrant + bge-m3); SearXNG is NOT wired. ops/deep_research.py does not exist. CLEAN.
- F9 (HITL review queue): not introduced. CLEAN.
- F12 / F13 / F11: no multi-format ingest, no cascade delete, no clipper. CLEAN.

### chat() stub confirmation

chat() is STUBBED on all 3 backends per §5 of the scope lock. Architect review §1 confirms:
`ollama.py:90`, `api.py:120`, `cli.py:97` all raise `NotImplementedError(... v0.4 (F6))` with
no network call. Signature is locked per ABC. Full F6 implementation is v0.4. CONFIRMED.

### No hardcoded provider/model IDs

Architect review §1 confirms the only claude-* strings in app/ are in column/field doc
comments and an Alembic seed default that is env-overridable
(`os.environ.get("DEFAULT_MODEL_ID", "claude-sonnet-4-6")`). The only base_url literal is
`https://api.anthropic.com`, confined to `provider/api.py` and env-overridable via
ANTHROPIC_BASE_URL. No API key literals anywhere; all keys via os.environ inside api.py/cli.py
only. CONFIRMED per CLAUDE.md §12 / I6.

### Early extras (not scope violations)

One early extra noted: the `/provider/config/{config_id}` DELETE endpoint. The scope lock
required GET and POST; DELETE is a logical extension of the CRUD surface and does not introduce
any out-of-scope feature ID. Classified as a harmless early extra. Logged here for velocity
calibration; does not require escalation.

---

## 4. Cost tracking (I7 / §11 confirmation)

- ingest_runs table has `total_cost_usd Numeric(10,4)` column; written on every ingest run
  (both orchestrated and delegated paths) per architect review §2.
- $1.00/run anomaly threshold: `cost_anomaly` bool flag set + WARNING logged inline after
  the row write (AQ-v0.2-8, ADR-0009 §4). Verified by T-LOOP-* tests.
- Sandbox cost: $0.00 (no live inference calls in CI). This is correct and expected.
- Real cost will be logged to ingest_runs on the M2 TrueNAS smoke run (EC-M2-5/17).
- CLI provider records total_cost_usd = 0.00 by convention (uses build-time agent credits,
  not runtime billing); documented in ADR-0009. tokens=0 + WARNING emitted when SDK metadata
  is absent.
- CLAUDE.md §11 cost tracking: I7 COMPLIED.

---

## 5. Non-blocking follow-ups for v0.2.1 / v0.3

These three items do NOT block M2 sign-off. They are recorded here so they survive into the
next sprint.

| ID | Owner | Item | Target |
|----|-------|------|--------|
| NB-1 | backend-engineer | Widen fallback except clause to include `httpx.HTTPStatusError` for 5xx responses (currently catches only TimeoutError + ConnectionError; a literal HTTP 503 from httpx would bypass the single fallback and surface as IngestError without engaging the fallback per ADR-0009 §4). Filed by architect review §2. | v0.3 hardening |
| NB-2 | backend-engineer | T-CQ-009 I6 guard is a coarse whole-file substring check for the word "InferenceProvider". This forced avoidance of that word in a main.py docstring — the guard should be scoped to import statement lines only (e.g., `grep "^from\|^import" backend/app/`) rather than scanning all text. Zero functional impact; the guard still blocks accidental imports. | v0.3 hardening |
| NB-3 | backend-engineer | openapi.json info.version is "0.1.0" and info.description still says "walking skeleton (M1)" — stale strings set in backend/app/main.py. Routes and schemas are v0.2-accurate. Tech-writer confirmed non-blocking. Update version string and description before the v0.2 final commit. | v0.2 final commit (before merge) |

Note on DIAG-NB-1: the architect filed a cosmetic label-drift item (component.mmd cited
ingest/config.py, ingest/parser/wikilinks.py, ingest/catalogue.py as illustrative paths,
whereas the implementation lives at provider_config_service.py, wiki/links.py, wiki/index.py).
The task brief confirms this is now FIXED by tech-writer. Not carried forward.

Note on NB-3: this item is a pre-merge action for the current sprint, not a v0.3 carry-forward.
backend-engineer should resolve it before merging sprint/v0.2.

---

## 6. Human checkpoint — remaining conditions (EC-M2-17)

The following conditions must be satisfied by Emanuele on the TrueNAS / M2 environment before
Sprint 3 begins. No code change is required; these are runtime/live verification steps.

### Condition A — 3-provider live smoke matrix (EC-M2-5 / EC-M2-6)

Environment prerequisites:
- OLLAMA_URL set to running Ollama on TrueNAS; model capable of JSON output configured in
  provider_config (global row, provider_type='local')
- ANTHROPIC_API_KEY set; ApiProvider global row configured
- claude-agent-sdk installed and accessible in PATH; CliAgentProvider global row configured
- Postgres 16 running; Alembic migrations applied (`alembic upgrade head`)
- Qdrant running; bge-m3 embedding service reachable (EMBEDDING_URL set)

Run commands:
```
# Option 1 — pytest live suite (runs all 3 providers sequentially)
cd backend
pytest tests/test_smoke_providers.py -m live -v

# Option 2 — standalone smoke script (more verbose output)
SYNAPSE_SMOKE_MOCK=0 python scripts/smoke_providers.py
```

Pass criteria per provider (asserted by the harness):
- At least 1 schema-valid WikiPage written to vault/wiki/ with type, title, sources[] all
  non-empty and lang field present (F3)
- vault/wiki/overview.md created or updated (K2/F3)
- vault/wiki/index.md contains [[wikilink]] entry for each generated page (K3)
- ingest_runs row present with total_cost_usd >= 0.00; Local/CLI = 0.00; API > 0.00 (I7)
- Correct routing: Local/API → orchestrated loop; CLI → delegated via MCP tools (F17)
- All wikilinks in generated pages have rows in the links table (K5)

### Condition B — Obsidian vault open (EC-M2-13)

After the smoke matrix has produced real LLM-generated pages:
1. Open vault/wiki/ in Obsidian (any recent version).
2. Confirm: no error dialogs on open.
3. Confirm: all generated pages are visible in the file explorer.
4. Confirm: [[wikilinks]] within generated pages resolve to other generated pages (graph view
   or click-through in reading mode).
5. Confirm: index.md and overview.md open without parse errors.

### Condition C — Explicit sprint advancement approval

After completing A and B, Emanuele explicitly approves:
"Sprint 3 (v0.3 / M3 — 'Graph live, no main-thread freeze') is approved to begin."

This confirmation is the final gate before the orchestrator unlocks Sprint 3 scope.

---

## 7. BACKLOG.md update note

BACKLOG.md Sprint 2 items are updated to done / done-pending-live-smoke status in the
accompanying BACKLOG.md commit. Velocity note is recorded there.

---

## 8. Velocity note

v0.2 was estimated at 10–14 solo evening sessions (the highest-complexity sprint in the
roadmap per v0.2-scope §11). Delivery included:

Delivered as scoped: all 11 in-scope feature IDs + 5 D-artifact families fully complete.

Early extras: 1 (DELETE /provider/config/{id} endpoint — trivial, no feature ID, no
out-of-scope risk). ADRs 0010 and 0011 were additional (beyond the 3 required); both
document real decisions made this sprint and count as velocity, not overrun.

Bug found during QA: BUG-v0.2-1 (delegate_ingest return type). Found and fixed within the
sprint. Does not count as scope overrun.

Verdict: ON SCOPE. No underrun (all IDs delivered). No overrun (no out-of-scope feature ID
shipped). The early extras are micro-additions consistent with clean API design. The two
additional ADRs reflect genuine architectural decisions that required documentation.

The mmdc CI render check (AC-D3-3) is the only deferred item that was explicitly listed as
"best-effort" in the scope lock §11 velocity note. Carried forward to v0.3 devops tasks.

v0.3 boundary (F4 graph) is INTACT. No graph code shipped.

---

## 9. MILESTONE VERDICT

**MILESTONE M2: MET-PENDING-HUMAN-CHECKPOINT**

Definition: All 17 exit criteria are either fully verified (13/17) or verified at the
mock/automated level with a documented live-run condition (4/17 pending-live). Zero exit
criteria are NOT MET. Zero invariant violations. All 4 agent gate sign-offs are positive.

The milestone advances to the human checkpoint stage per CLAUDE.md §8 DoD:
"green tests + architect review + docs gate + PM exit-criteria sign-off + human checkpoint
before next sprint."

The first four are complete. The fifth (Emanuele's confirmation) is gated on the live
TrueNAS smoke run described in §6 above.

Sprint 3 (v0.3 / M3 — "Graph live, no main-thread freeze") MUST NOT begin until EC-M2-17
is satisfied.

**Remaining conditions for full M2 CLOSED:**
1. NB-3: backend-engineer updates openapi.json info.version / info.description before merge.
2. Live smoke matrix (EC-M2-5/6): `pytest tests/test_smoke_providers.py -m live -v` passes
   on TrueNAS for all 3 providers.
3. Open vault/wiki/ in Obsidian after real pages generated (EC-M2-13).
4. Emanuele explicitly approves Sprint 3 (EC-M2-17).

Signed: product-manager · 2026-06-28
