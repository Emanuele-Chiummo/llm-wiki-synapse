# Sprint 1 — v0.1 — Scope Lock
> Milestone: M1 — "Data flows end-to-end"
> Branch: sprint/v0.1
> Locked by: product-manager on 2026-06-28
> Status: LOCKED — no changes without PM + orchestrator approval

---

## 1. Sprint objective

Prove that the data pipeline is correctly wired: a Markdown file dropped into
vault/raw/sources/ is detected by the watcher, indexed into Postgres (metadata)
and Qdrant (bge-m3 vector), and retrievable via the REST API. No AI inference,
no graph layout, no frontend UI.

The walking skeleton establishes the foundation that every later sprint builds on.
It is intentionally thin.

---

## 2. In-scope feature IDs

| Feature ID | What is in scope for v0.1 | Why needed now |
|------------|--------------------------|----------------|
| K1 | vault directory skeleton: raw/sources/, raw/assets/, wiki/ (index.md, log.md), schema.md, purpose.md stub | All later features assume this layout exists |
| K4 | log.md append-only history — INDEXED event type only | Required by I1: log is the parse source for incremental refresh decisions; must exist from day 1 |
| K6 | YAML frontmatter parser (type, title, sources[]); store fields in Postgres | Pivot for Postgres metadata; required for D2 ER to be meaningful |
| K7 | vault/wiki/.obsidian/app.json auto-generated on startup; valid frontmatter in all service-written files | Invariant I5 is non-negotiable; must hold from sprint 1 |
| F16 (partial) | dataVersion integer in vault_state table; bumped on each ingest; exposed via GET /status | Required by I2 architecture (graph recompute trigger) even though graph is not built yet. Full F16 deferred. |
| K2 (partial) | REST read API only: GET /status, GET /pages, GET /pages/{id}, POST /ingest/trigger | The "end-to-end" demo requires a way to observe that data arrived; ingest operation itself (LLM) deferred |
| Watcher | backend/app/watcher.py — watchdog-based incremental file detector; upserts Postgres + Qdrant | The entire M1 goal depends on this working correctly and honouring I1 |
| Postgres models | backend/app/models.py — SQLAlchemy 2; tables: pages, vault_state; Alembic migrations | D2 is generated from models.py; correct schema is the contract for all agents |
| Qdrant integration | Upsert/delete bge-m3 vectors for each indexed page; reuses running instance (I9) | Half of "data flows end-to-end" is the vector store |
| Docker Compose | docker-compose.yml with synapse-backend + postgres16; Makefile targets: up, test, er, openapi | Required to satisfy I8 (reproducible build) and for QA/devops sign-off |
| D1 | C4 context + container diagrams in docs/architecture/ (Mermaid) | Required by I8 docs-as-DoD; v0.1 is D1's first sprint per CLAUDE.md §9 |
| D2 | ER diagram docs/er/schema.mmd generated by `make er` | Required by I8; must match live schema; first sprint per CLAUDE.md §9 |
| D4 | OpenAPI JSON docs/api/openapi.json generated by `make openapi` | Required by I8; first sprint per CLAUDE.md §9 |

---

## 3. Explicitly deferred feature IDs

The items below are tempting but out of scope for v0.1. Any engineer proposing one
must escalate to orchestrator for explicit approval before work begins.

| Feature ID | Why tempting | Why deferred | Target sprint |
|------------|-------------|--------------|---------------|
| F17 | Defines Synapse; I6 is a non-negotiable invariant | v0.1 has no LLM inference calls at all; the InferenceProvider ABC exists only as a stub import if needed for type annotations. Full implementation in v0.2. | v0.2 |
| F3 | Two-step CoT ingest (analyze → generate) | Requires F17 providers to exist. No AI in v0.1. | v0.2 |
| K3 | index.md auto-maintained by ingest | Requires ingest loop (F3 + F17). Stub file created manually in v0.1. | v0.2 |
| K5 | [[wikilink]] parser | Not needed until graph edges are computed (F4). | v0.2 |
| K8 | Human curates / review queue | Principle applied; F9 implementation deferred. | v0.5 |
| F4 | Knowledge graph, 4-signal weighting, FA2, sigma.js | Requires pages in DB first (v0.1 provides that), but graph engine and viewer are v0.3 work. | v0.3 |
| F1 | 3-panel web UI | No frontend in v0.1. | v0.4 |
| F2 | purpose.md injection | Requires provider layer. | v0.6 |
| F5 | 4-phase RAG retrieval | Requires graph (F4) and chat (F6). | v0.5 |
| F6 | Multi-conversation chat | No chat at all in v0.1. | v0.4 |
| F7 | think reasoning display | Depends on chat streaming. | v0.4 |
| F8 | LaTeX to Unicode | Depends on chat. | v0.4 |
| F9 | HITL review queue | v0.5 feature. | v0.5 |
| F10 | Deep Research (SearXNG loop) | v0.5 feature. | v0.5 |
| F11 | Chrome MV3 clipper | v0.6 feature. | v0.6 |
| F12 | Multi-format ingest | Markdown .md only in v0.1; PDF/DOCX/etc in v0.5. | v0.5 |
| F13 | Cascade deletion | Requires graph + wikilink parser. | v0.5 |
| F14 | Configurable context window | Requires provider layer. | v0.4 |
| F15 (most) | PWA + Tauri v2, CI/CD pipeline | Docker Compose only in v0.1; CI pipeline and PWA in v0.6. | v0.6 |
| F16 (rest) | i18n, settings, .obsidian full auto-gen, GFM, multi-provider chat, timeout | Only dataVersion column is in scope for v0.1. | v0.4 |
| MCP server | FastMCP standalone server | v0.2 deliverable. | v0.2 |
| D3 | Sequence diagrams | Not required until v0.2 (ingest loop) and v0.3 (graph). | v0.2/v0.3 |
| D5 | UI screenshots via Playwright | No UI in v0.1. | v0.3 |
| D6 | User guide + Deploy guide | v0.4 draft. | v0.4 |
| D7 | Architecture Decision Records | Required from v0.2 per CLAUDE.md §9. | v0.2 |

---

## 4. Invariants in force for Sprint 1

All 9 invariants apply from day 1. The ones most directly tested by v0.1:

| Invariant | How it manifests in v0.1 | Test / verification |
|-----------|--------------------------|---------------------|
| I1 — INCREMENTAL INDEX ONLY | Watcher uses mtime + content hash; skip unchanged files; NEVER rescans directory on startup | pytest: drop same file twice → single DB row |
| I5 — OBSIDIAN COMPATIBILITY | vault/wiki/.obsidian/app.json auto-created; all written .md files have valid YAML frontmatter | Manual: open vault/wiki/ in Obsidian; no errors |
| I7 — LOOPS ARE BOUNDED | No loops in v0.1 (no LLM inference). The file-event handler is not a loop. | N/A in v0.1; architecture decisions made now must not preclude bounds later |
| I8 — DOCS-AS-DoD | D1+D2+D4 required before M1 sign-off; `make er` and `make openapi` are automated | CI: make er + make openapi must exit 0 with correct output |
| I9 — DO NOT REINVENT | bge-m3 via existing running service (env var URL); Qdrant via existing running instance; no new services introduced | docker-compose.yml must NOT start a new Qdrant or embedding container |

Invariants I2, I3, I4, I6 are not directly exercised in v0.1 (no graph, no chat,
no editor, no provider routing), but no design decision in v0.1 may make them harder
to honour in v0.2+. Any such conflict must be escalated to solution-architect.

---

## 5. M1 Exit Criteria (concrete, checkable)

All items must be MET before PM sign-off and before advancing to Sprint 2.

### EC-1 (K1 + K4 + K6 + K7): Vault structure valid
- vault/raw/sources/, vault/raw/assets/ exist.
- vault/wiki/index.md, vault/wiki/log.md exist with valid YAML frontmatter.
- vault/wiki/.obsidian/app.json is present and parseable as valid JSON.
- vault/schema.md and vault/purpose.md (stub) exist.
- Opening vault/wiki/ in Obsidian shows no errors (human checkpoint).

### EC-2 (Watcher + I1): Incremental ingest fires correctly
- Drop a new .md file into vault/raw/sources/ → within 5 seconds: a row appears in Postgres pages table AND a point appears in Qdrant synapse_pages collection.
- Drop the same unchanged file again → no new Postgres row, no new Qdrant point, no new log.md entry (I1).
- Modify the file → Postgres row updated (updated_at changes), Qdrant vector replaced, log.md gets one new INDEXED entry.
- Delete the file → Postgres row has deleted_at set, Qdrant point removed.
- Service startup does NOT trigger any DB writes (no rescan).

### EC-3 (K4): log.md append-only
- After 3 distinct ingest events, log.md has exactly 3 entries.
- File is never shorter than after the previous run (append-only contract).

### EC-4 (K6 + Postgres): Frontmatter stored correctly
- A .md file with frontmatter type: entity, title: "Test", sources: ["a.pdf"] produces a Postgres row with those exact values.
- A .md file with missing frontmatter produces a row with NULL for missing fields (no exception).

### EC-5 (Qdrant + I9): Vectors from existing bge-m3 service
- Qdrant synapse_pages collection exists after service startup.
- After ingest, a point with correct payload is retrievable via Qdrant API.
- EMBEDDING_URL env var is the only way the service reaches bge-m3; no local model loaded.

### EC-6 (REST API): All 4 endpoints operational
- GET /status → HTTP 200, JSON with data_version field.
- GET /pages → HTTP 200, paginated list; new page visible after ingest.
- GET /pages/{id} → HTTP 200 for valid id; HTTP 404 for unknown id.
- POST /ingest/trigger {"file_path": "..."} → HTTP 202; triggers ingest pipeline.

### EC-7 (F16 dataVersion): Version increments
- data_version starts at 0.
- After first ingest: data_version = 1 (GET /status confirms).
- After second ingest: data_version = 2.
- No ingest = no version change.

### EC-8 (Docker Compose + devtools):
- `docker compose up` on a clean environment starts synapse-backend and postgres16 without error.
- `make test` exits 0 with all pytest tests green.
- `make er` generates docs/er/schema.mmd.
- `make openapi` generates docs/api/openapi.json.
- No secrets in committed files (.env is gitignored).

### EC-9 (D1 — docs gate):
- docs/architecture/context.mmd is a valid Mermaid C4Context diagram.
- docs/architecture/container.mmd is a valid Mermaid C4Container diagram.
- Both reviewed and approved by solution-architect.

### EC-10 (D2 — docs gate):
- docs/er/schema.mmd is generated by `make er` (not hand-written).
- ER diagram matches live Postgres schema (pages + vault_state tables with all columns).
- Reviewed and approved by solution-architect.

### EC-11 (D4 — docs gate):
- docs/api/openapi.json is valid OpenAPI 3.1.
- All 4 v0.1 endpoints documented with request/response schemas.
- Generated by `make openapi`.
- Reviewed and approved by tech-writer.

### EC-12 (QA gate):
- QA-test-engineer has run the full test suite and signed off (green).
- No open P0 or P1 bugs.

### EC-13 (Architect gate):
- solution-architect has reviewed models.py, watcher.py, main.py and signed off.
- No invariant violations identified.

### EC-14 (Tech-Writer gate):
- tech-writer has verified D1, D2, D4 are complete and internally consistent.
- Signed off.

### EC-15 (Human checkpoint):
- Human (Emanuele) has reviewed the demo (EC-2 live run) and approved advancement to Sprint 2.

---

## 6. Required D-artifacts for this sprint's docs gate

| Artifact | Path | How generated | Gate requirement |
|----------|------|---------------|------------------|
| D1 | docs/architecture/context.mmd + container.mmd | Hand-authored Mermaid C4 by tech-writer | Architect + tech-writer sign-off |
| D2 | docs/er/schema.mmd | `make er` from models.py | Must match live schema; architect + tech-writer sign-off |
| D4 | docs/api/openapi.json | `make openapi` from FastAPI | Tech-writer sign-off; all 4 endpoints present |

### D-artifacts explicitly NOT required for v0.1

| Artifact | First required sprint |
|----------|-----------------------|
| D3 — Sequence diagrams | v0.2 (ingest loop) |
| D5 — UI screenshots | v0.3 (graph viewer) |
| D6 — User/Deploy guides | v0.4 (draft) |
| D7 — ADRs | v0.2 (first ADR: InferenceProvider) |

---

## 7. Sign-off register (to be completed at sprint end)

| Gate | Owner | Status | Date |
|------|-------|--------|------|
| QA green | qa-test-engineer | PENDING | — |
| Architect review | solution-architect | PENDING | — |
| Tech-writer docs gate | tech-writer | PENDING | — |
| PM exit-criteria verdict | product-manager | PENDING | — |
| Human checkpoint | Emanuele | PENDING | — |

All 5 must be MET before sprint/v0.1 is merged and Sprint 2 begins.

---

## 8. Velocity note

Sprint 1 is intentionally minimal: walking skeleton only, no AI inference, no frontend.
Estimated scope for a solo evening developer: 8–12 evening sessions (~2 weeks per roadmap).
If any EC item slips, the human checkpoint is the final gate before scope creep can enter Sprint 2.
