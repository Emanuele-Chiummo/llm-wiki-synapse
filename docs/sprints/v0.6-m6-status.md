# M6 / v0.6 → v1.0 — Build Status & Exit-Criteria Tracker

> Author: orchestrator (autonomous build)
> Date: 2026-06-30
> Branch: `sprint/v0.6` (descends from `sprint/v0.5`)
> Verdict: **CODE-COMPLETE — PENDING EC-M6-HCP** (human checkpoint required before the `v1.0.0` tag)
> Baseline at cut: backend 926 pytest / frontend 621 vitest. Now: **backend 968 / frontend 711**, all green.

This file tracks the M6 "Shippable" milestone toward the **v1.0.0** tag. It is the single
source of truth for what is done, what remains, and the gate that separates "code-complete"
from "v1.0 released".

---

## 1. Feature delivery (this branch)

| Feature | Deliverable | Commit | Verified |
|---------|-------------|--------|----------|
| **K2** Lint-fix loop (3rd core op) | `ops/lint.py` bounded HITL loop + 6 `/lint/*` endpoints + migration 0014 + ADR-0037 + `lint-fix.mmd` | `745600f` | pytest 946; I6/I7/I1 static tests |
| **K2** Lint UI | `LintView` + `lintStore` + `lintClient` + nav slot + i18n EN/IT | `ac90d35` | vitest 680; tsc+eslint clean |
| **F11** Web clipper + secure ingress | MV3 `extension/` (Readability+Turndown) + hardened `POST /clip` + ADR-0038 + `web-clip.mmd` | `7c91354` | pytest 968; 22 security tests (401/403/413/safe-join) |
| **F15** CI gate | tests run in CI, `sprint/**` trigger, pinned ruff/black, mmdc check | `8bbd2a9` | ruff/black clean tree-wide; pins 0.15.20 / 24.1.0 |
| **F15** PWA | manifest + offline app-shell SW (API NetworkOnly) | `cc365f6` | vite build emits sw.js; vitest 711 |
| **F15** Tauri v2 shell | `src-tauri/` scaffold + tag-only multi-OS CI build + ADR-0039 | `f68b2ad` | cargo resolves; config valid; **native build CI-only** |
| **F2** purpose.md context | already wired (ingest `orchestrator.py:961`, chat `context.py:86`) | (pre-existing) | `test_chat::test_includes_purpose_and_overview` |
| audit reference | `docs/reference/llm_wiki-audit/*` (parity input) | `c1cdc29` | — |

**Invariants:** I1, I2, I3, I4, I5, I6, I7, I8, I9 all held across every commit (no "never"-list
violation introduced). F11's secure ingress is the deliberate inverse of the reference app's
unauthenticated clip server (audit S-1…S-6).

---

## 2. Exit-criteria (EC-M6) status

| EC | Description | Status |
|----|-------------|--------|
| EC-M6-1 | K2 complete (backend + frontend), ACs green | **MET** |
| EC-M6-2 | F11 clipper + secure ingress, security ACs proven by tests | **MET** |
| EC-M6-3 | F15: PWA + Tauri shell; CI runs tests + pinned linters + mmdc on right branches | **MET** (Tauri *native binary* build is CI-only / live-verified) |
| EC-M6-4 | F2 verified; deferred FUs disposed | **PARTIAL** — F2 MET; FU disposal in §3 |
| EC-M6-5 | Full suite green (pytest + vitest); linters clean tree-wide | **MET** (968 / 711; ruff+black+tsc+eslint clean) |
| EC-M6-6 | D1–D7 complete & consistent; ER zero-drift; ADR index complete | **PARTIAL** — see §4 |
| EC-M6-7 | `wiki/` valid Obsidian vault after all M6 ops (I5/K7) | **MET** (no vault-format change; test_obsidian_check unaffected) |
| EC-M6-HCP | Human checkpoint (live build + browser/desktop verification + D5 screenshots) | **PENDING** — see §5 |

---

## 3. Deferred follow-up disposition

| FU | Item | Status |
|----|------|--------|
| GAP-v0.5-3 | mmdc Mermaid CI render check | **DONE** (CI docs job) |
| NB-9 | CI branch filter | **DONE** (`sprint/**`) |
| AC-F15-4 | linter drift cleared + pinned | **DONE** |
| FU-P4-3 | ADR-0026 cascade_delete non-atomic / idempotent-on-retry note (or single-session wrap) | **OPEN** — low-effort doc note; recommend ADR amendment |
| FU-P4-4 | F13 `files_written == len(rewrites)` contract (`==` vs `<=`) | **OPEN** — confirm/align AC |
| NB-7 / NB-8 | cosmetic `.mmd` label fixes | **OPEN** — P3 cosmetic |
| FU-P3-2 | USER.md note: CLI path does not enqueue reviews | **OPEN** — docs |

These OPEN items are non-blocking for code-correctness; they are docs/cosmetic and fold into
the final docs gate. None gate the HCP except as noted in §4.

---

## 4. Docs gate (D1–D7) remaining

- **D2 (ER)** zero-drift — maintained each migration (0014 added; `make er` run by the lint agent). ✓
- **D4 (OpenAPI)** — regenerated for `/lint/*` and `/clip`. ✓
- **D3 (sequences)** — `lint-fix.mmd`, `web-clip.mmd` added. ✓ (NB-7/NB-8 cosmetic open)
- **D7 (ADR)** — 0037/0038/0039 added + indexed. ✓
- **D6 (USER/DEPLOY)** — USER.md (PWA) + DEPLOY.md (desktop) updated. **TODO:** add `LINT_*` and `CLIP_*` env vars to the DEPLOY.md env table; FU-P3-2 note.
- **D5 (screenshots)** — **PENDING-LIVE**: Lint view, clipper flow, PWA install, desktop window — captured during the HCP session (`make screenshots` / Playwright against a live stack). Carries forward the M5 D5-pending items too.
- **DOCS_STATUS.md / BACKLOG.md / TRACEABILITY.md** — **TODO:** roll up the M6 entries.
- **MkDocs** — optional; not started (explicitly optional per scope §2).

---

## 5. EC-M6-HCP — what the human checkpoint requires (blocks the v1.0.0 tag)

The `v1.0.0` tag is intentionally **not** applied autonomously. Per the project DoD and because
these need a real environment (TrueNAS stack + browser + OS runners), the following must be done
by Emanuele (or on live CI) before tagging:

1. **Live stack smoke** — `docker compose up`, point at Ollama/Qdrant/bge-m3, run a real ingest →
   verify wiki pages, graph, chat with citations, lint scan, and a real web-clip → auto-ingest.
2. **PWA** — install the built frontend as an app; confirm offline app-shell loads and API calls
   are never served stale.
3. **Tauri desktop** — let the tag-only CI matrix build the macOS/Windows/Linux binaries (or build
   locally with WebKit libs); launch and smoke-test the desktop window.
4. **D5 screenshots** — capture the M5 + M6 pending PNGs into `docs/screens/`.
5. **3-provider matrix** — smoke ingest+chat on Ollama / API / CLI (I6).
6. Sign off the four gate chains (QA / architect / tech-writer / PM) per the established process.

On HCP pass → roll up DOCS_STATUS/BACKLOG/TRACEABILITY, bump versions to **1.0.0**
(`frontend/package.json`, `src-tauri/tauri.conf.json`, `backend/pyproject.toml`), and tag **v1.0.0**.

---

## 6. Remaining autonomous work (no human needed) before HCP

Small, safe, can be done next without a live environment:
- FU-P4-3 ADR-0026 atomicity note; FU-P4-4 AC alignment; NB-7/NB-8 `.mmd` cosmetics.
- DEPLOY.md env table: add `LINT_*` + `CLIP_*`; FU-P3-2 USER.md note.
- DOCS_STATUS.md + BACKLOG.md + TRACEABILITY.md M6 roll-up.
- (optional) MkDocs site.

After those, the only thing between this branch and `v1.0.0` is **EC-M6-HCP** (§5).
