# v0.5 — Architect Review — M5 Phase 2 (Deep Research, F10)

- **Reviewer:** solution-architect
- **Date:** 2026-06-29
- **Branch:** `sprint/v0.5`
- **Scope:** F10 Deep Research, judged against the frozen contract ADR-0024 and the 9 invariants.
- **Headline invariant:** I7 (bounded loops).
- **Method:** static read of the implementation against ADR-0024 §2/§3/§4/§5/§7/§8. Test suite NOT run (QA owns that, in parallel).

## Verdict: APPROVE-WITH-CONDITIONS

The core of F10 — the bounded loop, the SearXNG-only web-search seam, the ingest-seam synthesis, and the data model — is implemented exactly to the frozen contract. All 11 Do-NOT items are HELD in the loop module. The boundedness posture (I7) is provable from the code: `for iteration in range(1, max_iter_local + 1)`, a top-of-round budget gate, and a single hardcoded `Semaphore(3)`.

**One non-P0 correctness defect blocks unconditional approval:** the REST entry `POST /research/start` and the loop `run_deep_research` each generate a **different** `run_id` and each INSERT a row, so the loop writes to a row the client never polls. This does not violate any invariant (every loop path is still bounded; both rows are still terminal-written), but it breaks the fire-and-poll contract of ADR-0024 §8.1 and would make the UI poll a permanently-`running` orphan. It must be fixed before the phase gate. See Condition C1.

---

## Do-NOT compliance table (ADR-0024 §5)

| # | Do-NOT (P0 = 1–4) | Verdict | Evidence |
|---|---|---|---|
| 1 | No `while True` / unbounded refinement; must be `for … range(1, max_iter+1)` | **HELD** | `deep_research.py:187` — `for iteration in range(1, max_iter_local + 1):`. No `while` anywhere in the loop body. Two internal `break`s only (budget, converged). |
| 2 | No re-reading bounds/config/env mid-loop (frozen at start) | **HELD** | Bounds frozen into locals before the loop: `max_iter_local = run_obj.max_iter` (`:175`), `token_budget_local = run_obj.token_budget` (`:176`). `_mq = _max_queries()` read once at `:179`. The loop body reads only these locals + `accumulator.total_tokens`; no `settings.`/`run_obj` access inside `range`. |
| 3 | No web-search backend other than SearXNG; no fallback | **HELD** | All search routed through `searxng_search_many` (`deep_research.py:346` → `searxng.py:100`). `searxng.py` is the sole HTTP-search site; base URL from `settings.searxng_url` only (`:50`); non-200/timeout → `[]`, never an alternative engine (`:61–71`). Static guard `test_no_forbidden_search_imports` exists (`test_deep_research.py:913`) and a live grep of `ops/` returns zero forbidden tokens. |
| 4 | No exceeding concurrency=3 | **HELD** | Single module `_semaphore = asyncio.Semaphore(CONCURRENCY)` with `CONCURRENCY = 3` (`searxng.py:28,31`). Both search (`searxng.py:112`) and fetch (`deep_research.py:366`) acquire that **same** semaphore. No second semaphore, no unbounded `gather` (the `gather`s fan out only over already-semaphore-guarded coroutines). |
| 5 | No direct write to `vault/wiki/`; no direct `generate()` — synthesis via `ingest_file()` | **HELD** | `_ingest_synthesis` writes to `raw/sources/deep-research-<run_id>.md` (`:521–527`) then calls `ingest_file(abs_path)` (`:533`). No `provider.generate()` / `write_wiki_page()` call in the module. |
| 6 | No hardcoded provider/model/key; no `isinstance` branch on provider | **HELD** | `_resolve_provider` uses `resolve_provider_config("ingest", vault_id)` + `resolve_provider(config_row)` (`:686–700`). No model id / base_url / key literal. No `isinstance(provider, …)` anywhere. (The one `isinstance` at `:308` tests `prior_context`, a `str`/`list` arg — not the provider.) |
| 7 | Never leave a run "running"; terminal write in `finally`, → error on exception | **HELD** | `status` pessimistic default `"max_iter_reached"` (`:164–166`); `except` sets `"error"` + `error_message` (`:232–236`); `_finalize_run_row(...)` is in `finally` (`:238–251`) and always overwrites `status` + `completed_at`. No code path leaves the row's status `"running"`. (Caveat: applies to the row the **loop** owns — see C1.) |
| 8 | Never skip sufficiency-before-refine | **HELD** | `_assess_sufficiency` is called every round (`:211`) **before** the refine branch (`:220–224`). On the final iteration the refine branch is skipped by `if iteration < max_iter_local`, so the loop exits with the assessment already done. |
| 9 | No per-source secondary loop | **HELD** | `_fetch_and_extract` is single-pass: one `httpx.get` per hit, failure → `content_md=None`, logged & dropped (`:365–392`). No retry/re-fetch loop. |
| 10 | No new InferenceProvider ABC method (rides `chat()`) | **HELD** | All three inference phases call `provider.chat(messages=…, retrieval_context=…)` (`:326`, `:432`, `:491`). No `research()`/`assess()` added to the ABC; `base.py` untouched by F10. |
| 11 | No `data_version` double-bump (ingest_file does the single bump) | **HELD** | No `bump_version()` / `data_version` reference in `deep_research.py`. The single bump happens inside the re-entrant `ingest_file` (ADR-0003 seam). |

**P0 items 1–4: all HELD.** No headline-invariant violation.

---

## Structural-bounds confirmation (the three bounds, provable from code)

| Bound | Structural enforcement | Confirmed |
|---|---|---|
| `max_iter` (iteration cap) | `for iteration in range(1, max_iter_local + 1)` at `deep_research.py:187`. The iteration count is capped by the range header; no statement in the body extends it. Boundedness confirmable by reading one line. | **YES** |
| `token_budget` (spend gate) | `if accumulator.total_tokens >= token_budget_local: status = "budget_exhausted"; break` at the **top** of each round before any provider/network spend (`:192–194`). Conservative direction (under-spend). | **YES** |
| `concurrency = 3` (fan-out ceiling) | Hardcoded `CONCURRENCY = 3` → `_semaphore = asyncio.Semaphore(CONCURRENCY)` (`searxng.py:28,31`); the *single* semaphore is acquired by both search and fetch. Architect-gate comment present. | **YES** |

Bounds are frozen on the `deep_research_runs` row at INSERT (model `max_iter`/`token_budget` NOT NULL, no server default — must be supplied) and read once into locals; the loop never re-reads config. The "frozen at run start" guarantee (AQ-v0.5-4) holds inside the loop.

---

## Data-model / migration conformance (ADR-0024 §7)

| Check | Result |
|---|---|
| `deep_research_runs` columns match §7.1 | **MATCH** — all 16 columns present with correct types/nullability (`models.py:980–1128`; migration `0009:58–122`). |
| `deep_research_sources` columns match §7.2 | **MATCH** — all 7 columns present (`models.py:1131–1206`; migration `0009:131–162`). |
| FK CASCADE on child | **PRESENT** — `ForeignKey("deep_research_runs.id", ondelete="CASCADE")` in both model (`:1152`) and migration (`0009:143`). ORM relationship `cascade="all, delete-orphan"` (`:1114`). |
| `synthesis_page_id` FK → pages.id | **PRESENT** — model `:1086`, migration `0009:111`. |
| Indexes | **PRESENT** — `ix_deep_research_runs_vault_started (vault_id, started_at)` and `ix_deep_research_sources_run_id (run_id)` in both model `__table_args__` and migration (`0009:124–128, 164–168`). |
| UUID/JSONB `.with_variant` for SQLite test parity does NOT change Postgres production types | **CONFIRMED** — model uses `UUID(as_uuid=True).with_variant(String(36), "sqlite")` and `JSONB().with_variant(JSON(), "sqlite")`; the migration (the production DDL authority) emits `postgresql.UUID` and `postgresql.JSONB` unconditionally (`0009:60–66, 84–88`). Postgres production types are unaffected by the SQLite variant. |
| Migration chain | `0009` revises `0008` (`0009:50–51`). Numbering correction (F10=0009 before F9=0010) per ADR-0024 §7 note is respected. |

`make er` regeneration (D2) and `make openapi` (D4) are I8 gate items — flagged to tech-writer; not re-run here. **Condition C3.**

---

## I9 static guard

The code **cannot** reach Tavily/Google/DDG/SerpAPI:

- The only outbound search call is `searxng.py:searxng_search` → `GET {SEARXNG_URL}/search?format=json`. No second search function exists.
- `searxng.py` imports only `httpx`, `pydantic`, `app.config`. No search SaaS SDK.
- The static guard `test_no_forbidden_search_imports` (`test_deep_research.py:913`) scans all `app/ops/**/*.py` against `tavily|duckduckgo|ddg|googlesearch|serpapi|google[-_]search[-_]results`. Live grep of `ops/` returns **zero** matches, so the guard passes and the ops docstrings themselves don't trip it.
- On SearXNG failure: `[]` returned + logged (`searxng.py:52, 62, 66`). No fallback engine. `POST /research/start` returns **503** when `SEARXNG_URL` is unset (`main.py:2240–2247`) — no fake run, no fallback. **HELD.**

---

## I3 / frontend polling

| Check | Result |
|---|---|
| Polling is a `setTimeout` chain, not `setInterval` | **HELD** — `researchStore.ts:243, 252`. No `setInterval`. |
| Chain stops on terminal status | **HELD** — `tick()` returns early when `isTerminal(detail.status)` (`:218`); next tick scheduled only `if (!isTerminal(updated.status))` (`:242`). |
| Cleanup on unmount / navigate-away | **HELD** — `AbortController` aborted via returned cleanup (`:254`); `DeepSearchView` calls it on unmount and on terminal (`DeepSearchView.tsx:652–666`); `tick` bails if `selectedRunId !== runId` (`:217`) or `ctrl.signal.aborted` (`:214`). |
| No main-thread heavy work / tight loop | **HELD** — 5s interval; selectors + `useShallow` on collections (`:518`); synthesis rendered as static `<pre>` (no per-token markdown parse), consistent with I3. List virtualised with TanStack Virtual (I4, `:527`). |

---

## Conditions

### C1 — `run_id` divergence (BLOCKING, non-P0, correctness)

`POST /research/start` generates `run_id = uuid.uuid4()` (`main.py:2251`), pre-INSERTs a `deep_research_runs` row with that id (`:2266–2285`), returns it to the client (`:2305`), then schedules `run_deep_research(vault_id=…, topic=…, max_iter=…, token_budget=…)` **without passing `run_id`** (`:2288–2295`). Inside `run_deep_research`, a **new** `run_id = uuid.uuid4()` is created (`deep_research.py:146`) and `_create_run_row` INSERTs a **second** row (`:147–153`, `:553–588`).

Consequences:
- The client polls the endpoint's row, which the loop never updates → it stays `status="running"` forever. This defeats the fire-and-poll contract (ADR-0024 §8.1) and is a soft I7-observability regression (a row that never reaches terminal, even though the actual loop is bounded and its own row IS finalized).
- Two rows per run pollute `GET /research/runs` and double-count `sources_fetched=0` orphans.

**Required fix (pick one, architect-preferred = A):**
- **A.** `run_deep_research` accepts `run_id: uuid.UUID` and `_create_run_row` becomes an UPDATE-or-skip (the endpoint already INSERTed). The endpoint passes its `run_id`. The loop reads `max_iter`/`token_budget` from the existing row (preserving "frozen on the row" semantics). This keeps a single source of truth and the 202-then-poll guarantee.
- **B.** The endpoint does NOT pre-INSERT; it lets `run_deep_research` own the INSERT and returns the loop's `run_id`. Requires awaiting the row creation before returning 202 (a short await, not the loop) so the poll target exists. More refactoring; A is cleaner.

Either way: exactly **one** row per run, and the id returned to the client is the id the loop finalizes.

### C2 — confirm the QA loop-gate tests bind to the fixed entry (advisory)

The mandatory `max_iter_reached` / `budget_exhausted` / `error-never-running` tests (`test_deep_research.py:322, 468, 746`) call `run_deep_research` directly, so they pass regardless of C1. Add/confirm one `test_research_api.py` assertion that **`POST /research/start` then `GET /research/runs/{id}` eventually reaches a terminal status** — this is the regression that would have caught C1. Hand to QA.

### C3 — D-artifact gate (I8, blocking for phase DoD, owned with tech-writer)

Before the phase gate: `make er` (D2 — both tables + relationships in `docs/er/schema.mmd`), `make openapi` (D4 — the three `/research/*` endpoints), and `docs/sequences/deep-research.mmd` (D3, ADR-0024 §9) must be regenerated and committed with zero drift. Not verified in this review; flagged to tech-writer.

---

## Invariant tension the ADR did not anticipate

- **Mechanical (no-provider) path.** `_resolve_provider` returning `None` (no `provider_config` row) is handled gracefully — query-gen falls back to `[topic]`, assess treats any content as sufficient, synthesize assembles raw snippets (`:303–306, 411–414, 465–472`). ADR-0024 assumed a resolved provider. This is a reasonable, invariant-safe degradation (still bounded, still SearXNG-only, still ingest-seam), but it means a deep-research run can complete with **zero** LLM involvement and still write a `raw/sources/` doc. Acceptable for Phase 2; note it in USER.md so users understand an unconfigured-provider run produces a low-quality stub. No invariant traded.
- **`_create_run_row` returns an expunged snapshot** (`:587`) and the loop never re-attaches it — correct, and it's the mechanism that makes "frozen on the row" hold without lazy re-reads. Good. (This is also why C1's fix must keep reading bounds from the row, not from the function args, to preserve the audit semantics.)

No invariant is traded for convenience. The two P0 headline invariants (I7, I9) are fully held in the implementation.

---

## Handoff

- **C1** → backend-engineer (run_id unification in `main.py` + `deep_research.py`). Re-review required before phase gate.
- **C2** → qa-test-engineer (add the start→poll→terminal API regression).
- **C3** → tech-writer (D2/D3/D4 regeneration + ADR-0024 README row).
- Phase-2 verdict → orchestrator: **APPROVE-WITH-CONDITIONS** (C1 blocking).
