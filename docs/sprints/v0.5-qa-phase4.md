# QA Gate — v0.5 Phase 4 (F13 Cascade Deletion)
> Author: qa-test-engineer (claude-sonnet-4-6)
> Date: 2026-06-29
> Branch: sprint/v0.5
> Verdict: FAIL — 2 open defects block milestone sign-off

---

## Verdict summary

**FAIL — milestone NOT MET.**

Two open defects prevent sign-off:

| ID | Severity | Summary |
|----|----------|---------|
| DEFECT-F13-001 | P1 — I1 violation | `_method_c_fulltext` invoked unconditionally on the links-table-hit happy path (ADR-0026 §3.1 architect-review gate not satisfied) |
| DEFECT-F13-002 | P1 — I5 violation | `frontmatter.dumps()` reorders YAML keys alphabetically, corrupting frontmatter structure on every dead-wikilink rewrite and every `sources[]` prune |

Both are documented as `xfail` tests in the test suite (see §5 below). The milestone may not advance until both are fixed and the xfail tests flip to pass.

---

## 1. Baseline verification

| Suite | Run 1 | Run 2 | Flakes |
|-------|-------|-------|--------|
| Backend pytest | 672 passed, 3 skipped, 2 xfailed | 672 passed, 3 skipped, 2 xfailed | None |
| Frontend vitest | 526 passed (28 files) | 526 passed (28 files) | None |

The 2 xfailed are the QA-added defect-tracking tests (T-CD-024b, T-CD-026). They are expected failures, not regressions.

---

## 2. AC coverage map

| AC | Description | Test(s) | Status |
|----|-------------|---------|--------|
| AC-F13-1 | cascade_delete executes all 7 steps in order | T-CD-009 (soft-delete), T-CD-010 (Qdrant), T-CD-007/T-CD-013 (rewrite), T-CD-015 (index), T-CD-011 (data_version) | PASS |
| AC-F13-2 | 3-method matching; method label logged | T-CD-002 (method a), test_method_a_result_reflected_in_plan; method b via slug tests; method c via T-CD-026 (xfail — method c invoked when it should not be) | PARTIAL — DEFECT-F13-001 |
| AC-F13-3 | Shared-entity warnings advisory; deletion proceeds | T-CD-016 | PASS |
| AC-F13-4 | I1: only referencing files written; data_version +1 once; no FA2 | T-CD-011 (bump exactly once), T-CD-012 (no FA2), T-CD-013/T-CD-021 (files_written == rewrites), T-CD-026 (xfail — method c called) | PARTIAL — DEFECT-F13-001 |
| AC-F13-5 | DELETE /pages/{id} → 200 correct shape; 404 on double-delete | T-CD-018, T-CD-019, T-CD-019b, T-CD-020 | PASS |
| AC-F13-6 | Frontend: two-step modal; shared-entity warnings before confirm; cancel/ESC/backdrop no-op | CascadeDeleteModal.test.tsx (all cancel/ESC/backdrop/confirm/back tests) | PASS |
| AC-F13-7 | 3 scenarios: 3 rewrites, shared-overlap, double-delete | T-CD-021, T-CD-016, T-CD-019b | PASS |

---

## 3. Test quality assessment

### 3.1 Dead-wikilink rewrite (I5 frontmatter integrity)

**What the original tests cover:**
- T-CD-007 (`test_dead_wikilink_plain_rewrite`): real-filesystem test that writes `ref.md`, calls `cascade_delete`, reads back the file, and asserts `[[Target]]` is gone and parsed YAML keys (`title`, `type`) are correct. This is NOT mock-only. The assertion is on parsed metadata values.

**What was missing:**
- No test asserted byte-identical frontmatter. The ADR §4.3 claims "byte-for-byte via `frontmatter.dumps`" but this is false — PyYAML sorts keys alphabetically on dumps.

**QA addition (T-CD-024 — `test_frontmatter_integrity_after_rewrite`):**
- Writes a multi-key frontmatter file with non-alphabetical key order.
- Calls `cascade_delete` on a real tmp_path.
- Reads file back and asserts: (a) parsed key values unchanged, (b) dead links gone, (c) unrelated links untouched.
- Status: **PASS** (semantic integrity confirmed).

**QA addition (T-CD-024b — `test_frontmatter_key_order_preserved_xfail`):**
- Same setup, additionally asserts the raw frontmatter block bytes are identical.
- Status: **XFAIL (strict)** — confirms DEFECT-F13-002.
- The assert produces: original `"title: Ref Order\ntype: concept\nsources:\n  - raw/sources/doc.md\ntags:\n  - alpha\ncreated_at: '2026-01-01T00:00:00Z'\n"` vs. after `"created_at: '2026-01-01T00:00:00Z'\nsources:\n- raw/sources/doc.md\ntags:\n- alpha\ntitle: Ref Order\ntype: concept\n"`.
- Note also: list item indentation changes (`  - item` → `- item`). This is a second formatting drift that can cause noisy diffs in Obsidian git history.

### 3.2 Preserve-shared sources[] mutation (real filesystem)

**What the original test covers (T-CD-005 — `test_multi_source_page_in_will_preserve`):**
- Creates `concept.md` with `sources: [doc.md, other.md]` in `tmp_path`.
- Asserts `wiki_id not in delete_point_ids` (preserved = not Qdrant-deleted).
- Does NOT read `concept.md` back to verify the sources[] prune happened on disk.

**QA addition (T-CD-025 — `test_preserve_shared_sources_pruned_in_file`):**
- Same setup.
- After `cascade_delete`, reads `concept.md` from disk.
- Asserts: (a) `raw/sources/doc.md` removed from `sources[]`, (b) `raw/sources/other.md` still present, (c) `title` and `type` values intact, (d) `wiki_id` not deleted.
- Status: **PASS** — `_prune_sources` correctly writes to the real file and the prune is proven on the filesystem.
- Key finding: the existing T-CD-005 was sufficient to prove Qdrant behavior but the file-level mutation was untested. T-CD-025 closes this gap and confirms the implementation is correct.
- Note: T-CD-025 also exhibits DEFECT-F13-002 (key order reordering) on `concept.md` since `_prune_sources` uses the same `frontmatter.dumps()` path, but the semantic values are correct.

### 3.3 Method (c) not invoked on links-table-hit (I1)

**What the original test inventory states:**
- T-CD-004 is listed in the inventory comment at line 20 but has no corresponding test body.

**QA addition (T-CD-026 — `test_method_c_not_called_on_links_table_hit`):**
- Patches `_method_c_fulltext` and asserts it is not called when method (a) finds the reference.
- Status: **XFAIL (strict)** — confirms DEFECT-F13-001.
- Root cause: `_build_wikilink_rewrites` calls `_method_c_fulltext` unconditionally at every invocation. The comment in the production code says "We still run (c) to catch hand-edited files absent from the links table" which contradicts ADR-0026 §3.1: "(c) is skipped entirely when (a)∪(b) already covers the candidate set". The ADR §9 (architect-review gate) explicitly requires this as a precondition for PR approval.

---

## 4. Gate-specific checks

### 4.1 I1 — No full-rescan

- **Static proof:** `grep -n "glob\|walk\|listdir\|scandir" cascade_delete.py` returns zero results. No vault walk in the production code.
- **Primary path (methods a/b):** References are found exclusively via `SELECT ... FROM links WHERE target_page_id = :id OR target_title = :T` (method a) and slug-normalized comparison over `links` rows (method b). Both are pure index reads.
- **Method (c) fallback:** Bounded by `CASCADE_FULLTEXT_MAX_FILES=5000`. When invoked, it queries the `pages` table (not a filesystem walk) to enumerate live wiki pages, then reads each file once. This is I1-safe by the ADR §3.1 definition.
- **DEFECT-F13-001:** The fallback is invoked unconditionally, even when the links-table hit is complete. This is a behavioral deviation from ADR §3.1 (extra SQL query to enumerate live wiki pages on the happy path). Not a hard I1 violation (no vault walk; references are not computed from the walk) but violates the ADR architect-review gate.
- **Verdict on I1:** Functionally correct (no full-rescan used to identify changes); ADR §3.1 happy-path gate not met.

### 4.2 I2 — data_version bumped exactly once; no inline FA2

- T-CD-011: `_bump_version_and_notify` called exactly once (mock call count = 1). PASS.
- T-CD-012: `GraphEngine` constructor not called. PASS.
- Code inspection: `cascade_delete` imports `_graph_cache.notify_bump()` (debounced) not `GraphEngine.recompute()`. PASS.
- `_bump_version_and_notify` wraps bump + `notify_bump()`. No FA2 or `recompute()` call path. PASS.

### 4.3 Dry-run (POST preview) read-only

- T-CD-001b (`test_preview_does_not_modify_vault_state`): intercepts UPDATE statements on `vault_state` and asserts zero calls during `plan_cascade_delete`. PASS.
- T-CD-017 (`test_preview_returns_200`): API test asserts 200 with correct shape. PASS.
- T-CD-020 (`test_preview_404_on_unknown`): 404 when page not found. PASS.
- Code inspection: `plan_cascade_delete` reads only (`select` statements); no `update`, no file write, no `delete_point` call. PASS.

### 4.4 Incremental data plane (soft-delete; no hard delete; Qdrant hard-delete; raw file deleted)

- T-CD-009 (`test_soft_delete_update_called`): intercepts SQL, asserts `UPDATE pages` (not `DELETE FROM pages`). PASS.
- T-CD-010 (`test_qdrant_delete_point_called`): asserts `delete_point` called once per page in `will_delete`. PASS.
- T-CD-014 (`test_raw_source_file_deleted`): creates real file in `tmp_path/raw/sources/`, asserts it does not exist after delete. PASS.
- Double-delete (T-CD-019b): second DELETE returns 404 (idempotent). PASS.

### 4.5 Frontend two-step flow

- Loading state: `cascade-delete-preview-loading` rendered; confirm button disabled. PASS.
- Preview error: error text shown; confirm disabled. PASS.
- Shared-entity warnings rendered BEFORE other sections (DOM order checked with `compareDocumentPosition`). PASS.
- Warnings repeated on step 2 (confirm screen). PASS.
- Cancel button: `onCancel` called; `cascadeDelete` not called. PASS.
- ESC key: `onCancel` called; `cascadeDelete` not called. PASS.
- Backdrop click: `onCancel` called; `cascadeDelete` not called. PASS.
- Confirm button on step 2: `cascadeDelete(pageId)` called; `onDeleted(result)` fired. PASS.
- Error on DELETE: inline error shown; `onDeleted` not called. PASS.
- Back button: returns to step 1 (preview). PASS.

### 4.6 D2 unchanged; D4 has both new endpoints

- Last Alembic migration: `0010_review_items.py` (F9). No F13 migration. PASS.
- `docs/er/schema.mmd` contains `deleted_at` (pre-existing). No new columns. PASS.
- `docs/api/openapi.json` contains both `/pages/{page_id}/cascade-delete/preview` (POST) and `/pages/{page_id}` (DELETE) with full descriptions. PASS.

---

## 5. Defect register

### DEFECT-F13-001 — P1 — I1: method (c) called on happy path

- **Filed against:** backend-engineer
- **Location:** `backend/app/ops/cascade_delete.py`, function `_build_wikilink_rewrites`, lines 500-503
- **Description:** `_method_c_fulltext` is called unconditionally regardless of whether method (a) or (b) already found all references. On the happy path (links-table hit), this issues an extra SQL query to enumerate all live wiki pages from the `pages` table. While no file read or full vault walk occurs in this case (because `already_found` filters all candidates), the extra DB query violates the ADR §3.1 gate: "(c) is skipped entirely when (a)∪(b) already covers the candidate set".
- **ADR reference:** ADR-0026 §3.1 and §9 (architect-review gate bullet 2).
- **Test:** T-CD-026 (`test_method_c_not_called_on_links_table_hit`) — xfail(strict).
- **Fix:** Add an early-return guard before the `_method_c_fulltext` call. Simplest implementation: `if all_found_ids and not b_results: return rewrites, match_methods_used` (skip (c) when (a) found hits and (b) found nothing new). A more complete guard would check whether all live wiki pages enumerable by the pages table are a subset of `all_found_ids`, but the common case (links table is up-to-date) is captured by the simpler guard.
- **Risk:** Without the guard, every cascade delete issues an extra paginated SQL query. With `CASCADE_FULLTEXT_MAX_FILES=5000` this can be significant on large vaults.

### DEFECT-F13-002 — P1 — I5: frontmatter key order corrupted by PyYAML sort

- **Filed against:** backend-engineer
- **Location:** `backend/app/ops/cascade_delete.py`, all uses of `frontmatter.dumps()` in `cascade_delete()` (step 4, line 667) and `_prune_sources()` (line 808)
- **Description:** `python-frontmatter` uses PyYAML's default `Dumper` which sorts mapping keys alphabetically. This means any file rewritten by F13 has its YAML frontmatter key order changed, regardless of the author's original ordering. Observed: `title, type, sources, tags, created_at` → `created_at, sources, tags, title, type`. Also: list items lose the 2-space indent (`  - item` → `- item`). Both changes are semantically equivalent but create unnecessary diffs in Obsidian's git plugin, violate the user's formatting preferences, and break any key-order-dependent tooling.
- **ADR reference:** ADR-0026 §4.3: "The frontmatter block is re-emitted byte-for-byte via `frontmatter.dumps`". The claim is false.
- **Test:** T-CD-024b (`test_frontmatter_key_order_preserved_xfail`) — xfail(strict); T-CD-024 (`test_frontmatter_integrity_after_rewrite`) — PASS (semantic values correct).
- **Fix options (in preference order):**
  1. Switch to `python-frontmatter` with `ruamel.yaml` handler: `frontmatter.loads(raw, handler=frontmatter.YAMLHandler())` using a `ruamel.yaml` `CommentedMap` preserves key order and list formatting.
  2. Alternative: read the raw frontmatter block as a string, extract and replace only the `sources:` key value when pruning, leaving all other keys byte-identical (body-regex approach, limited to the specific key being mutated).
  3. Last resort: after `frontmatter.dumps()`, compare with original and only write if body actually changed; accept the key-order change as a one-time cost per file touched by F13.
- **Affected paths:** Every `WikilinkRewrite` file write (step 4) and every `_prune_sources()` call (step 5). Files that are NOT touched by F13 are unaffected.

---

## 6. Obsidian compatibility check

The Obsidian check (`test_obsidian_check.py`) was not run against a live vault in this gate (no vault data available in CI). Static check:
- DEFECT-F13-002 means that after a cascade delete, any rewritten wiki file will have its YAML keys sorted. `test_obsidian_check.py` validates frontmatter YAML syntax (parseable), not key order. So the check would still pass syntactically but the vault will show unnecessary git diffs.
- No dead `[[wikilinks]]` will remain after a cascade delete (T-CD-021 proves this on real files). Obsidian check gate would pass on that dimension once DEFECT-F13-001 and DEFECT-F13-002 are fixed.

---

## 7. Flakiness report

Both suites run twice with identical results. No flakes detected. The 3 pre-existing skips in pytest are unrelated to F13. The 2 xfailed tests are deterministic (strict xfail).

The 3 pre-existing warnings in pytest (`RuntimeWarning: Event loop is closed` in test_mcp_tools.py) are carry-forward from prior sprints and unrelated to F13.

---

## 8. Handoffs

| To | Item |
|----|------|
| backend-engineer | DEFECT-F13-001: add early-return guard in `_build_wikilink_rewrites` before `_method_c_fulltext` call when `all_found_ids` is non-empty |
| backend-engineer | DEFECT-F13-002: replace `frontmatter.dumps()` with a key-order-preserving serializer (ruamel.yaml recommended) in `cascade_delete` step 4 and `_prune_sources` step 5 |
| orchestrator | Phase 4 gate: FAIL. Milestone must not advance until both defects are fixed and T-CD-024b + T-CD-026 flip from xfail to pass |
| product-manager | M5 Phase 4 is NOT signed off. DEFECT-F13-001 (I1 gate) and DEFECT-F13-002 (I5 gate) are P1 blockers |

---

## 9. Tests added by QA gate

Three tests added to `backend/tests/test_cascade_delete.py` in `TestQARealFilesystem`:

| Test ID | Function | Purpose | Status |
|---------|----------|---------|--------|
| T-CD-024 | `test_frontmatter_integrity_after_rewrite` | Real-filesystem I5 proof: semantic values correct after rewrite | PASS |
| T-CD-024b | `test_frontmatter_key_order_preserved_xfail` | I5 byte-identical gate (documents DEFECT-F13-002) | XFAIL strict |
| T-CD-025 | `test_preserve_shared_sources_pruned_in_file` | Real-filesystem preserve-shared proof: sources[] pruned on disk | PASS |
| T-CD-026 | `test_method_c_not_called_on_links_table_hit` | I1 architect-review gate: method (c) skipped on happy path (documents DEFECT-F13-001) | XFAIL strict |
