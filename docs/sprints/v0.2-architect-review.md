# Sprint 2 — v0.2 (M2) — Architect PR Review + F17 Invariant + Diagram Sign-off

> Reviewer: solution-architect
> Date: 2026-06-28
> Milestone: M2 — "Agentic loop closed, 3 providers"
> Gate: EC-M2-15 (architect sign-off on D3 + D7 + D1u); the I6/I7 invariant gate
> Scope: implementation review of F17 against `docs/sprints/v0.2-architecture.md` +
>   ADR-0007..0011, and the 9 invariants in CLAUDE.md §3. Code was NOT modified.
> Authority: this verdict gates the merge of `sprint/v0.2`.

This review verifies the IMPLEMENTATION against the design I signed off in
`v0.2-architecture.md §9`. It is a per-item APPROVE/BLOCK gate. Verdict at §10.

---

## 1. I6 — Pluggable inference (capability-aware routing, no hardcode) — **APPROVE**

| Check | Evidence | Result |
|-------|----------|--------|
| Routing decides on `capabilities().supports_agentic_loop` ONLY | `orchestrator.run_ingest_pipeline()` reads `caps = provider.capabilities()` then `if caps.supports_agentic_loop:` (orchestrator.py:313/322). No other signal gates the branch. | PASS |
| No `isinstance` / `type()` / class-name / `provider_type ==` in the routing path | grep of `orchestrator.py` + `loop.py`: the only `isinstance` hits are unrelated (`page.type` PageType check in the validator, `_sources_raw` list coercion). No `type(`, `__class__`, `provider_type ==`, or `.name ==` branch anywhere in routing. | PASS |
| `resolve_provider` has NO hardcoded default backend, raises on unknown/missing type | `provider/__init__.py:68` — `None` row → `ValueError` ("no hardcoded default"); unknown `provider_type` → `ValueError` listing valid types. `_REGISTRY` is a config-value→class dispatch (the row's `provider_type`), explicitly NOT a routing decision. | PASS |
| Resolution order operation > vault > global | `provider_config_service.resolve_provider_config()` queries scope='operation'+vault+op, then scope='vault'+vault, then scope='global', each with `is_fallback IS FALSE`. `ConfigNotFoundError` on no match — never a silent default (I6). | PASS |
| No model id / API key / base_url literal outside provider_config rows + env | grep across `app/`: the only `claude-*` strings are in **column/field doc comments** (models.py:264, main.py:207 descriptions) and the **Alembic seed default** (`os.environ.get("DEFAULT_MODEL_ID", "claude-sonnet-4-6")`). The only base_url literal is `https://api.anthropic.com`, confined to `provider/api.py` and env-overridable (`ANTHROPIC_BASE_URL`). No key literals; all keys via `os.environ` inside `api.py`/`cli.py` only. | PASS |
| `chat()` is a locked stub (NotImplementedError) on all 3 backends | `ollama.py:90`, `api.py:120`, `cli.py:97` all raise `NotImplementedError(... v0.4 (F6))` with no network call. Signature locked per ABC. | PASS |
| Provider selectable per operation/vault | `_resolve_ingest_provider_config()` resolves `operation='ingest'`; same `ConfigResolver` serves chat/lint scopes. Per-vault + per-operation rows honoured. | PASS |
| Backends are config, not new classes | OpenAI-compatible path is `base_url`-driven inside the single `ApiProvider` (`api.py:83` `_openai_compatible = bool(config.base_url)`). No subclass-per-vendor. | PASS |
| `provider_name`/`model_id` audit-only, never routed on | Stored on `ingest_runs` from `caps.name`/config; never read back into a routing decision. | PASS |

**I6 VERDICT: APPROVE.** Routing is single-signal capability-based with machine-checkable
guardrails. No backend is hardcoded; no provider literal escapes `provider/` + env + seeded DB
rows. The CLI delegated vs orchestrated separation is correct (`delegate_ingest` on the agentic
path; `analyze()`/`generate()` raise `RuntimeError` on `CliAgentProvider` to make accidental
orchestrated use loud — exceeds the design ask).

---

## 2. I7 — Bounded loops + cost logging — **APPROVE**

| Check | Evidence | Result |
|-------|----------|--------|
| Orchestrated loop bounded by BOTH max_iter AND token_budget | `loop.run_orchestrated_loop()`: `for i in range(1, max_iter+1)` (bound #1) with a pre-call `if accumulator.total_tokens >= token_budget: break` (bound #2). Both read from `provider_config` with the ADR-0009 defaults (3 / 60 000). | PASS |
| analyze() runs ONCE; only generate() retries (AQ-v0.2-1) | `analyze()` is called before the loop (loop.py:132); only `generate()` is inside the loop with augmented context. Matches ADR-0007 §4. | PASS |
| Non-convergence stops at max_iter, no overrun | Loop exits cleanly with `converged=False`, `stop_reason="max_iter"`; `iterations` capped at `max_iter`. AC-K2-5 honoured. | PASS |
| token_budget halts before an unaffordable call | Pre-call check sets `stop_reason="token_budget"` and breaks before `generate()`. Correct (analyze already spent budget). | PASS |
| `total_cost_usd` written to ingest_runs every run | `run_ingest_pipeline()` finalizes the accumulator and always `INSERT ingest_runs{...total_cost_usd...}` on both routes (orchestrated and delegated). `Numeric(10,4)`. | PASS |
| Fallback bounded to exactly once | `_run_orchestrated()` catches `(TimeoutError, ConnectionError)`, resolves ONE `is_fallback=True` row, retries once; a second failure raises `IngestError` with no chain (`from exc2`, no recursion). | PASS |
| $1/run anomaly → WARNING + cost_anomaly flag | `cost_anomaly = total_cost_usd > COST_ANOMALY_THRESHOLD_USD (1.00)`; flag persisted on the row; inline `logger.warning(...)` emitted AFTER the row write (AQ-v0.2-8). | PASS |
| Usage normalization across backends | Ollama→`prompt_eval_count`/`eval_count`, cost 0.0; Anthropic→`usage.input/output_tokens` × env price map; OpenAI→`prompt/completion_tokens`; CLI→SDK metadata, cost 0.0 by convention with tokens=0 + WARNING when absent. All push via `_record_usage` to the run-scoped `UsageAccumulator` (out-of-band, ADR-0007 §1). | PASS |
| CLI budget passed/abort | `CliAgentProvider` carries `config.token_budget` (100k default via `DEFAULT_TOKEN_BUDGET_CLI`); records exactly one `ingest_runs` row at the orchestrator. | PASS |

**I7 VERDICT: APPROVE.** Dual bounds enforced together; analyze-once retry-generate is exact;
fallback is exactly-once with no chaining; cost is persisted and anomaly-flagged on every run.

**Non-blocking note NB-1 (tracked, not a gate):** the fallback `except` clause catches
`(TimeoutError, ConnectionError)`. ADR-0009 §4 phrases the trigger as "timeout / 503". An
HTTP 503 surfaces from `httpx` as `HTTPStatusError` (via `raise_for_status()`), which is NOT a
subclass of those two — so a literal 503 would currently surface as `IngestError` WITHOUT
attempting the single fallback. This does not violate I7 (the loop and fallback remain bounded;
no overrun, no chain) and the timeout path — the primary failure mode — is covered. Recommend a
follow-up (v0.3 hardening) widening the catch to include `httpx.HTTPStatusError` for 5xx so the
fallback engages on 503 as the ADR intends. Filed to backend-engineer; does not block M2.

---

## 3. §12 — Secrets — **APPROVE**

| Check | Evidence | Result |
|-------|----------|--------|
| `provider_config` has NO api_key column | models.py `ProviderConfig` + migration 0002 `create_table("provider_config")` — no `api_key` column. ER `PROVIDER_CONFIG` has none. REST `ProviderConfigCreate`/`Response` (main.py) accept/return none. | PASS |
| API keys read from env only | `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` via `os.environ.get(...)` inside `api.py` and `cli.py` only; missing key → `ValueError` referencing §12/ADR-0008. | PASS |
| No secrets in code | grep for `sk-ant`/`sk-…`/`api_key = "…"` → none. Price map from `PROVIDER_PRICE_MAP` env (no price literals in code). | PASS |

**§12 VERDICT: APPROVE.** Keys are env-confined to `provider/`; nothing secret in DB, config,
ER, OpenAPI, or source.

---

## 4. Data model / D2 (ER + migration) — **APPROVE**

| Check | Evidence | Result |
|-------|----------|--------|
| 5 tables present | `models.py`: pages, vault_state, provider_config, ingest_runs, links. ER `schema.mmd` renders all 5. | PASS |
| provider_config columns match spec (ADR-0008 §2 / arch §5) | id, scope, operation(null), vault_id(null), provider_type, model_id, base_url(null), max_iter(def 3), token_budget(def 60000), is_fallback(def false), created_at, updated_at. No api_key. | PASS |
| ingest_runs columns match (I7) | id, vault_id, page_id(FK pages, null), provider_name, provider_type, model_id, route, max_iter_used, total_tokens, total_cost_usd `Numeric(10,4)`, converged, cost_anomaly, started_at, finished_at. | PASS |
| links columns match (K5) | id, source_page_id(FK pages, not null), target_title, target_page_id(FK pages, **nullable**), alias(null), dangling(def false), created_at. Two indexes on source/target. | PASS |
| Migration 0002 matches models | Column-by-column identical to `models.py` (types, nullability, server_defaults, FKs). One migration for all three tables (one schema-change event, I8). | PASS |
| Migration seeds from env, not hardcoded model ids | Global + fallback rows seeded with `model_id` from `os.environ.get("DEFAULT_MODEL_ID", ...)` / `DEFAULT_FALLBACK_MODEL_ID`. The literal is a fallback DEFAULT only; runtime app code always reads the DB row (AC-F17-8). | PASS |
| ER reflects reality (zero drift) | DOCS_STATUS reports `make er` zero drift; ER comments mirror model comments verbatim. | PASS |

**DATA-MODEL VERDICT: APPROVE.** Models, migration, and ER are mutually consistent and match
the design.

**Non-blocking note NB-2:** the seed global/fallback rows pin `provider_type='api'`. This is a
sensible default (API is the design's "default recommendation") and is user-editable via the
`/provider/config` CRUD; it is NOT a hardcode-in-code (it is a seed row). Acceptable. If the
operator runs offline-only they re-point the global row to `local`. No action required.

---

## 5. K5 / K3 + MCP — **APPROVE**

| Check | Evidence | Result |
|-------|----------|--------|
| K5 links parser + dangling resolution | `wiki/links.py`: regex `\[\[([^\[\]]+?)\]\]`; handles `[[T]]`, `[[T\|alias]]`, `[[T#sec]]`, dedupes; `persist_links()` DELETEs prior rows for the page then re-inserts (incremental, I1), bulk-resolves titles, sets `dangling=True` when unresolved — warn-not-error, does not invalidate (AQ-v0.2-7). | PASS |
| K3 index.md updater (I5 valid frontmatter) | `wiki/index.py`: writes `type: index` frontmatter block first (I5), groups live pages by type, excludes `overview`/`index`, idempotent (byte-stable for same DB state). | PASS |
| K3 is not a full rescan (I1) | `update_index()` reads the `pages` table (live rows) and rewrites one catalogue file per write event — it queries DB state, never re-walks the vault filesystem. Per-write, not per-vault-scan. | PASS |
| MCP write_page reuses the shared write path | `mcp/server.py write_page` → validates via the SAME `validate_pages()` (loop.py) → calls `write_wiki_page()` (orchestrator.py). No divergent write path; K5 parse + K3 index + Qdrant upsert + log all run identically (ADR-0010 §2). Returns structured `{"error": ...}` dicts, not exceptions. | PASS |
| search_wiki uses query_points (client ≥1.10) | `mcp/server.py:98` `await qdrant.query_points(... query=vector ...)` reading `query_response.points`; cosine score normalised to [0,1]. Legacy `.search(` removed (only referenced in a comment). Reuses bge-m3 + Qdrant (I9). | PASS |
| 4 tools registered | search_wiki, write_page, get_page, list_pages — all on the single `mcp` FastMCP instance; `list_pages` excludes soft-deleted; stdio entrypoint. | PASS |

**K5/K3/MCP VERDICT: APPROVE.** Single write path is honoured end-to-end; the orchestrated and
delegated (MCP) paths both flow through `write_wiki_page`, so I1/I5 hold identically. The
`query_points` migration is correct for qdrant-client ≥1.10.

**Observation O-1 (informational, not a defect):** the delegated path wires the MCP server via
`from app.mcp.server import mcp` and `CliAgentProvider.delegate_ingest` raises if `mcp_server is
None` — correctly refusing to let the agent fall back to raw filesystem writes that would bypass
I1/I5. The live SDK↔MCP wiring is exercised only under the deferred live smoke (EC-M2-5). The
seam is correct and safe-by-default; live verification stays a runtime/human gate.

---

## 6. Diagram sign-off (EC-M2-15 — resolves AC-D3 + D7 + D1u) — **APPROVE**

### D1 — C4 (context / container / component)

| Diagram | Accuracy check | Result |
|---------|----------------|--------|
| context.mmd | Line 1 `C4Context`. Shows Synapse, Emanuele, Ollama + Anthropic/OpenAI-compat + Qdrant + bge-m3 + Vault FS. Synapse description states capability-aware loop (I6) + bounded I7. Key via env (§12). **No v0.3+ drawn**: no graph/sigma, no SearXNG, no web UI. | APPROVE |
| container.mmd | Line 1 `C4Container`. InferenceProvider layer container ("the ONLY inference surface (I6); no model id/key/endpoint leaks; chat() stubbed") + MCP server (FastMCP, stdio) + Orchestrator ("ROUTES on supports_agentic_loop (I6)", "bounded by max_iter+token_budget (I7)"). Postgres lists all 5 tables; "No secrets stored (§12)". Routing shown as capability-based. | APPROVE |
| component.mmd | Line 1 `C4Component`. All 3 backends under the ABC; ConfigResolver (operation>vault>global), WikiPage validator, K5 parser, K3/F3 writer, MCP server (4 tools, write_page reuses primitives I1/I5). Orchestrator annotated "ROUTES on supports_agentic_loop (I6 — never isinstance/class)". Arrows: orch→prov(ABC)→3 backends; clip→mcp; no class/isinstance reference. | APPROVE |

Minor label drift (non-blocking): component.mmd cites file paths `ingest/config.py`,
`ingest/parser/wikilinks.py`, `ingest/catalogue.py` for ConfigResolver/K5/K3, whereas the
implementation lives at `provider_config_service.py`, `wiki/links.py`, `wiki/index.py`. The
COMPONENTS and their responsibilities are correct; only the illustrative path labels are stale.
This is a diagram-cosmetic note for tech-writer (does not affect topology accuracy or the gate).
Filed as **DIAG-NB-1**.

### D3 — sequence diagrams

| Diagram | Accuracy check | Result |
|---------|----------------|--------|
| ingest-routing.mmd | Line 1 `sequenceDiagram`. Both branches: `alt supports_agentic_loop == True (CLI — DELEGATED)` / `else == False (Local/API — ORCHESTRATED)`. Routing reads `ProviderCapabilities{supports_agentic_loop, ...}` only — no isinstance/type. ConfigResolver order shown. CLI path writes via MCP (validated, I5; persist + K5). `ingest_runs` INSERT + inline $1 anomaly at end, both branches. Matches `run_ingest_pipeline`. | APPROVE |
| ingest-loop.mmd | Line 1 `sequenceDiagram`. `analyze()` ONCE before the loop (AQ-v0.2-1). Loop label `iteration = 1..max_iter (stop when converged OR iteration==max_iter OR tokens>=budget)` — dual bound (I7). Pre-call `token_budget` check shown. validate → whole-batch retry with augmented context. converged + non-converged paths. `ingest_runs` INSERT + anomaly at end. Matches `run_orchestrated_loop`. | APPROVE |

### D2 — ER

5 tables, no api_key, I7 columns (total_cost_usd/converged/cost_anomaly), K5 links (nullable
target_page_id, dangling). Zero drift vs models.py. **APPROVE.**

### D7 — ADRs

ADR-0007 (ABC + analyze-once + validator + routing-via-capabilities), 0008 (provider_config +
ingest_runs + links, secrets-via-env, operation column), 0009 (max_iter=3, budgets, Usage
normalization, $1 anomaly inline, fallback-once), 0010 (stdio + shared write path), 0011
(locked schemas). All Accepted, indexed, and consistent with the code. **APPROVE.**

**DIAGRAM SIGN-OFF: APPROVED.** All D1/D2/D3/D7 artifacts are v0.2-accurate; no v0.3 graph/UI is
drawn as present; routing is shown as capability-based; the loop shows the I7 dual bounds.
Two cosmetic, non-blocking diagram notes (DIAG-NB-1 component path labels; the openapi.json
version/description string already tracked by tech-writer/backend) are handed to tech-writer.

---

## 7. Invariant sweep (I1, I5, I9 + others)

| Invariant | Result | Evidence |
|-----------|--------|----------|
| I1 incremental only | PASS | Source ingest keeps the mtime-then-hash gate; `write_wiki_page` upserts per page; K5 `persist_links` delete+reinsert per page; K3 `update_index` rewrites the catalogue from DB rows per write event. No vault rescan introduced. |
| I5 Obsidian valid | PASS | Typed `WikiFrontmatter` enforces type/title/sources/lang at the schema boundary; writer serializes enum as string value; index.md/overview.md carry valid frontmatter. |
| I6 | PASS | §1. |
| I7 | PASS | §2. |
| I8 docs-as-DoD | PASS | ADR-0007..0011 + C4 (3) + D3 (2) + ER + mcp-tools.json + openapi present; ER/openapi zero drift. |
| I9 do not reinvent | PASS | OllamaProvider reuses running Ollama; search_wiki reuses Qdrant + bge-m3; FastMCP for the server; no new inference/search service. |
| I2/I3/I4 | N/A | No graph/chat-UI/editor in v0.2; nothing introduced that breaks them. |

---

## 8. Blocking defects

**None.** No invariant is traded. The two notes (NB-1 fallback-on-503 catch width; NB-2 seed
provider_type) and the diagram-cosmetic note (DIAG-NB-1) are non-blocking and tracked.

---

## 9. Sign-off conditions from v0.2-architecture §9 — verification

| # | Condition | Result |
|---|-----------|--------|
| 1 | Routing branch contains no isinstance/type/class-name test | MET (§1) |
| 2 | No model id / key / endpoint outside provider/ | MET (§1, §3) — literals confined to seed-default + env-overridable Anthropic base + doc comments |
| 3 | `validate()` and `write_page` share the ONE validator | MET — both call `loop.validate_pages` (§5) |
| 4 | Non-converging loop stops at exactly max_iter=3 with converged=False | MET (§2) — QA AC-K2-5 green |
| 5 | D2 regenerated by `make er` matches live schema | MET — zero drift (§4, DOCS_STATUS) |

All five merge conditions are satisfied.

---

## 10. ARCHITECT VERDICT

**APPROVED.**

The F17 implementation conforms to the design signed off in `v0.2-architecture.md §9` and
honours all nine invariants. I6 (single-signal capability routing, no hardcoded backend, no
provider-literal leakage, env-only keys, locked chat() stubs) and I7 (dual-bounded loop,
analyze-once retry-generate, exactly-once fallback, per-run cost persistence + anomaly flag) —
the two gate invariants for M2 — are met with machine-checkable guardrails. Data model,
migration, and ER are consistent and drift-free. K5/K3 and the MCP write path correctly reuse
the single `write_wiki_page` seam, preserving I1/I5; `query_points` is correct for
qdrant-client ≥1.10. All D1/D2/D3/D7 diagrams are v0.2-accurate and signed off (EC-M2-15
cleared).

This sign-off covers the design+implementation+docs gate. The remaining M2 items —
EC-M2-5 (live 3-provider smoke on TrueNAS) and EC-M2-13/EC-M2-17 (open-in-Obsidian + human
checkpoint) — are runtime/human gates outside the architect's scope and remain correctly
deferred per the QA report.

Hand to orchestrator: **M2 architect gate MET** (pending the documented live-smoke + human
checkpoint). Three non-blocking follow-ups handed off:
- NB-1 → backend-engineer: widen fallback catch to include `httpx.HTTPStatusError` 5xx (503).
- DIAG-NB-1 → tech-writer: refresh component.mmd path labels (config/links/index modules).
- (already tracked) openapi.json `info.version`/`description` string → backend-engineer.

Signed: solution-architect · 2026-06-28
