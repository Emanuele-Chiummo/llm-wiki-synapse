# Milestone Gate Report — Sprint 1 / v0.1 / M1 "Data flows end-to-end"

**QA agent:** qa-test-engineer · **Date:** 2026-06-28 · **Branch:** `sprint/v0.1`

## Verdict

**M1 SIGN-OFF — CONDITIONALLY MET.** All automated gates are green. The conditions
that must be cleared by devops-engineer or by Emanuele (human gates) before M1 is
*fully* closed are listed in the DEFERRED block below.

## Test suite results

**Total: 156 tests / 156 passed / 0 failed / 0 skipped.** `total_cost_usd = $0.00`
(no AI inference in v0.1).

| File | Tests | Covers | Status |
|------|-------|--------|--------|
| `test_frontmatter.py` | 25 | AC-K6-1, AC-K6-2, AC-K6-3 | GREEN (pre-existing) |
| `test_watcher_hash.py` | 18 | AC-WATCH-2 (mtime+hash gate) | GREEN (pre-existing) |
| `test_vault_structure.py` | 15 | AC-K1-1..5, AC-K7-1..2 | GREEN (new) |
| `test_ingest_incremental.py` | 17 | AC-WATCH-1..6, AC-K4-1..3, AC-F16dv-2/4, I1/G1 | GREEN (new) |
| `test_api.py` | 21 | AC-REST-1..6, AC-F16dv-3, AC-D4-1..3 | GREEN (new) |
| `test_models_schema.py` | 28 | AC-K6-4, AC-F16dv-1, AC-PG-2..4, AC-D2-1..2 | GREEN (new) |
| `test_docs.py` | 22 | AC-D1-1..2, AC-D2-1..2, AC-D4-1..3 | GREEN (new) |
| `test_code_quality.py` | 10 | AC-DC-5, I6/I7/I9 static guards | GREEN (new) |

## Quality gates

- `ruff check app tests`: CLEAN (0 errors)
- `black --check app tests`: CLEAN (21 files unchanged)
- `mypy app` (py3.13): SUCCESS (0 issues, 11 source files)
- `make er` → `docs/er/schema.mmd`: PASS (erDiagram + PAGES + VAULT_STATE)
- `make openapi` → `docs/api/openapi.json`: PASS (OpenAPI 3.1, all 4 endpoints, 202 ingest-trigger)

## Invariant verification

- **I1 — Incremental only (G1):** unchanged file → no new DB row / no new Qdrant upsert
  (T-INC-003/004); pre-existing files at watcher start are NOT auto-indexed (T-INC-016);
  static scan confirms no `os.walk`/`os.listdir`/`rglob`/`glob` in executable code
  (T-INC-017, T-VAULT-013). **PASS.**
- **I5 — Obsidian:** `.obsidian/app.json` created, valid JSON, `legacyEditor=false`,
  idempotent; all wiki `.md` parse via frontmatter (T-VAULT-007..010). **PASS** (automated);
  AC-K7-3 manual gate pending (see DEFERRED).
- **I6 — No hardcoded provider in v0.1:** no provider instantiation in orchestrator;
  no InferenceProvider import in main (T-CQ-008/009). **PASS.**
- **I7 — Bounded loops:** no `while True:`; no loops exist in v0.1 (T-CQ-010). **PASS.**
- **I8 — Docs-as-DoD:** D1 C4 valid, D2 ER generated & complete, D4 OpenAPI complete. **PASS.**
- **I9 — No reinvention:** embedding model read from settings; Qdrant/bge-m3 reused. **PASS.**

## Performance gates

- **G1 (no full-rescan): PASS** (T-INC-003/016/017).
- G2 / G3 / G4: **N/A** — graph (v0.3), chat streaming (v0.4), virtualisation (v0.4).

## 3-provider smoke matrix

**N/A for v0.1** — no InferenceProvider exists yet (F17 seam is a commented extension
point in `orchestrator.py`). Mandatory from v0.2.

## Test infrastructure strategy (documented, not defects)

No Docker / live TrueNAS in the sandbox. Integration tests run infra-free using:
- **SQLite + aiosqlite** substituting Postgres (JSONB/UUID/TIMESTAMP → Text);
- **FakeQdrantClient** (in-process dict, async-wrapped);
- **FakeEmbeddingClient** (deterministic vector of configured dim; `call_count`/`last_text`).

Replacement contracts are documented in each test module's docstring; devops swaps stubs
for live infra in the CI integration stage on TrueNAS.

## DEFERRED items (clear before M1 is fully closed)

1. **AC-PG-1** — `alembic upgrade head` on live Postgres (devops, TrueNAS).
2. **AC-QD-2/3** — verify Qdrant point payload + vector on live bge-m3 (devops, TrueNAS).
3. **AC-DC-1** — full `docker compose up` integration (devops, TrueNAS).
4. **AC-K7-3** (GAP-1, MANUAL) — Emanuele opens `vault/wiki/` in Obsidian.
5. **AC-D1-3** (GAP-2, MANUAL) — solution-architect signs off C4.
6. **AC-D2-3** (GAP-3, MANUAL) — solution-architect signs off ER.
7. **EC-15** — Emanuele approves the M1 live demo.

## Gate verdict

**SIGN-OFF: MET (conditional).** No open P0/P1 defects. All v0.1-applicable automated
gates pass. Remaining items are live-infra verifications and human checkpoints, to be
cleared at deployment / the M1 live demo — none requires a code change.
