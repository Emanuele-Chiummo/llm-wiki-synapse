# Synapse 1.2.5 — chat su LAN http + catalogo TrueNAS 📡

## Fix
- **La chat funziona su `http://` in LAN**: `crypto.randomUUID` esiste solo nei secure context (https/localhost) — sostituito con un generatore UUID sicuro anche fuori da essi. Prima, inviare un messaggio da `http://truenas:5173` falliva in silenzio
- Formattazione ruff/black sul codice di configurazione S14–S18; header dell'ER generato senza data (il docs gate non dipende più dal giorno)

## Novità
- **Catalogo custom-app TrueNAS SCALE** (`trains/stable/synapse`): installazione one-click di Postgres+Qdrant+backend+frontend dalla UI Apps di TrueNAS, con logo, form guidato e aggiornamenti
