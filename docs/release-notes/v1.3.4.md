# Synapse v1.3.4 — Full UI Parity with llm_wiki

_Released: 2026-07-06_

Major parity release: a full screenshot-driven alignment of Lint, Chat, the knowledge graph, the Sources panel, MCP, and Deep Research with nashsu/llm_wiki, while retaining Synapse's superior internals (server-side FA2, incremental index, bounded loops). Governed by ADR-0058–0061. Verified live on a 986-page vault with ~2,000 backend and ~2,000 frontend tests; ruff/mypy/eslint/tsc clean; OpenAPI and ER diagram regenerated; migration 0025 single-head.

## Added

- **Lint parity with llm_wiki** [K2]:
  - Deterministic **broken-wikilink** category from dangling entries in the `links` table — zero LLM cost.
  - Tolerant suggested-target resolver + "Fix" button that rewrites the link in-body.
  - Batch bar (Fix / Ignore / Send-to-Review on selected items), per-row "Open" action, bridge to the Review queue.
  - Per-severity headers with true totals (e.g. "Warnings (259)"), semantic (LLM) lint toggle, Delete action for orphan pages.
- **Chat parity with llm_wiki** [F5, F6, F17]:
  - Attach image (capability-aware per provider, I6).
  - Web Search toggle (SearXNG; `[W]` citation namespace separate from `[n]`).
  - Retrieval-mode presets: Fast / Standard / Deep / Local-first (deterministic).
- **Graph header** — pages/links/hidden stats, Search in graph, Filter by type (I2-safe), Reset, fullscreen [F4].
- **Per-domain community names** — unique labels derived from the dominant domain (e.g. "SAM · Reconciliation"); coloured by Louvain cluster; Obsidian-style arc culling with chip reflecting the culled count; collapsible legend; Insights panel collapsed by default; centroid labels clamped to the canvas [F4, F18].
- **Sources & Reader** — folder import (recursive "+ Folder") + folder delete (cascade-bounded) + footer file count; reader "More" on tag overflow; `updated:` metadata row [F12, F16].
- **Wiki tab in Sources** — browse the `wiki/` folder structure read-only with preview, equivalent to llm_wiki's "Files" view [F1, K7].
- **MCP expanded from 4 to 9 tools**: `graph_neighborhood`, `list_reviews`, `resolve_review`, `read_source_file`, `trigger_rescan`; REST `review/bulk-resolve` + `PATCH` [F9, F17].
- **Installable Synapse Skill** (`tools/synapse-skill/`) — interrogate Synapse from Claude Code/Codex via an agent skill (read-only, trigger-disciplined) [F17].
- **Deep Research with LLM-optimized topic** — topic and queries are optimised by the LLM (reads `overview.md` + `purpose.md`) and presented in an editable confirm dialog before the run starts [F10].

## Fixed

- **Automations card true outcome** — the scheduled-automations card now reports the actual classification result (`dormant` / `error` / counts) instead of a blanket "ok" [F18, I7].

## Artifacts

`Synapse_1.3.4_aarch64.dmg` · `Synapse_1.3.4_x64-setup.exe` · `synapse-clipper-1.3.4.zip` · signed `latest.json` · GHCR images `backend`/`frontend`/`marker` `v1.3.4` + `latest`

---

**Full changelog**: https://github.com/Emanuele-Chiummo/llm-wiki-synapse/compare/v1.3.3...v1.3.4
