# Synapse v1.5.5 — remote MCP endpoint reachable again

_Released: 2026-07-13_

Patch: the remote MCP HTTP surface (`/mcp/server`) was never actually serving requests — Claude Desktop, claude.ai, and mcp-remote all got a 404. Two overlapping defects are fixed.

## Added

- **End-to-end MCP mount regression test** (`TestMcpServerMountedEndToEnd`) — POSTs an MCP `initialize` through the real ASGI stack, asserting the handshake reaches the app (200), no-slash → 307 → canonical, wrong bearer → 401, and remote-disabled → 404. Closes the coverage gap that let the regression ship green [F17].

## Fixed

- **`/mcp/server` was never mounted** — the R13-1 router split dropped the `app.mount()` block, so every remote MCP request hit a FastAPI routing 404 while `GET /mcp/info` still reported `http_enabled: true`. Restored the mount [F17].
- **Endpoint served at the wrong path** — `http_app()` defaults to `path="/mcp"`, so even once mounted the endpoint sat at `/mcp/server/mcp`. Now mounted with `http_app(path="/")` so it answers at `/mcp/server`, matching the docs, Settings UI snippet, and `/mcp/info.mount_path`. Clients may use `/mcp/server` (307 → canonical) or `/mcp/server/` directly [F17].

## Artifacts

`Synapse_1.5.5_aarch64.dmg` · `Synapse_1.5.5_x64-setup.exe` · `Synapse-1.5.5-portable-x64.exe` · `synapse-clipper-1.3.12.zip` · `Synapse_aarch64.app.tar.gz` · signed `latest.json`

---

**Full changelog**: https://github.com/Emanuele-Chiummo/llm-wiki-synapse/compare/v1.5.4...v1.5.5
