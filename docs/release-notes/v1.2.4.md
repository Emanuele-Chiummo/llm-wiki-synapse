# Synapse 1.2.4 — Impostazioni IA v2 + immagine frontend di produzione 🧭

## Novità
- **Impostazioni ripensate (ADR-0055)**: il pannello monolitico da 3.987 righe diventa una navigazione a due livelli con 16 pagine focalizzate (`settings/sections/*`) — stessa funzionalità, molta più chiarezza
- **Limiti dei loop modificabili dall'app**: `max_iter` e `token_budget` di deep-research e lint (S14–S18) ora si regolano da Opzioni, senza variabili d'ambiente (I7)
- **Immagine frontend di produzione**: build multi-stage Vite→nginx con reverse-proxy API integrato, pubblicata su GHCR ad ogni release — il web client si deploya con un `docker compose pull`

## Fix
- **Riclassificazione tipi**: ricorda le pagine già esaminate — niente doppia fatturazione AI sui run successivi, completamento reale

## Artefatti
`Synapse_1.2.4_aarch64.dmg` · `_x64-setup.exe` · `synapse-clipper-1.2.4.zip` · `latest.json` firmato · immagini GHCR `backend`/`frontend`/`marker` `v1.2.4`+`latest`
