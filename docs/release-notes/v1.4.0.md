# Synapse v1.4.0 — UI Parity & Secrets

_Released: 2026-07-10_

Introduces a 15-vendor provider catalog with one-toggle activation and model presets, encrypts all sensitive database secrets at rest (Fernet/AES-128-CBC+HMAC), makes the Marker PDF converter fully asynchronous, and adds a macOS menu-bar icon plus an in-app changelog. Requires Alembic migrations 0026 and 0027.

## Added

- **Provider vendor catalog** — one row per vendor for 15 known providers (Anthropic, Claude Code CLI, Codex CLI, OpenAI, Gemini, Azure, DeepSeek, Atlas, Groq, xAI, NVIDIA NIM, Kimi ×3, Ollama), each with a toggle, model presets, context-window and reasoning controls, and connection/function tests — matching the LLM Wiki "LLM Models" UX [F17].
- **Encrypted API key storage** — keys entered in the UI are encrypted at rest (Fernet/AES-128-CBC+HMAC, master key from `SYNAPSE_SECRET_KEY`); responses expose only `api_key_configured` + `api_key_masked`, never plaintext (migration 0026) [F17].
- **CLI auth co-located in its provider row** — the Claude Code CLI subscription OAuth token config now lives inside the Claude Code CLI vendor row; the Codex CLI row shows an inline auth note (`codex login` / `OPENAI_API_KEY`) instead of a separate section [F17].
- **Provider connectivity & function tests** — `POST /provider/test/{connection,function}`, bounded and never echoing the key [F17].
- **Async import UX** — the Marker convert panel shows a progress bar (N of M, %) + ETA + per-file status, a persisted conversion history with an "Open in Sources" button, and a fixed drag-and-drop zone [F12].
- **macOS menu-bar (system tray) icon** — a Synapse status-bar icon with "Open Synapse" / "Quit" and click-to-show, present while the app runs or is minimized [F15].
- **In-app Changelog** — Settings → Changelog renders this file as expandable per-version cards (10 most recent) [F16].

## Changed

- **Marker convert is now asynchronous** — `POST /ingest/convert-marker` returns `202 {batch_id, queued, total}` immediately and runs a serial background batch (status via `GET /ingest/convert-marker/status`), eliminating Cloudflare 524 timeouts on large PDFs [F12].
- **Provider settings rebuilt** — the add-a-provider-config form is replaced by the vendor-catalog UX; per-provider `reasoning_effort` supported, falling back to the env key when no stored key is set [F17].

## Fixed

- **ConvertPanel drag-and-drop** — the drop event now fires (`onDragEnter` preventDefault) [F12].
- **Vendor catalog stuck on "loading"** — removed an effect-dependency abort loop in the fetch [F17].

## Security

- **All sensitive DB secrets encrypted at rest** — `cli_oauth_token` (the `sk-ant-oat` subscription token) was stored in plaintext and is now Fernet-encrypted (migration 0027); provider API keys encrypted (migration 0026); `clip_access_token` confirmed already PBKDF2-hashed; no plaintext secret remains in the database [F17].

## Upgrade notes

- Run Alembic migration **0026** (encrypts existing provider API keys) and **0027** (encrypts existing CLI OAuth tokens). Both are additive and non-destructive; existing rows are re-encrypted in place.
- Set `SYNAPSE_SECRET_KEY` in your `.env` before migrating — the key must remain stable across restarts; rotating it invalidates stored encrypted values.

## Artifacts

`Synapse_1.4.0_aarch64.dmg` · `Synapse_1.4.0_x64-setup.exe` · `Synapse-1.4.0-portable-x64.exe` · `Synapse-1.4.0-unsigned.ipa` · `synapse-clipper-1.3.12.zip` · `Synapse_aarch64.app.tar.gz` · signed `latest.json`

---

**Full changelog**: https://github.com/Emanuele-Chiummo/llm-wiki-synapse/compare/v1.3.16...v1.4.0
