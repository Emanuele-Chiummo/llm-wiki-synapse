# Synapse v1.3.5 — Vault Keyfile Format Alignment

_Released: 2026-07-06_

Format-parity release aligning `schema.md` (stub → full llm_wiki contract) and `log.md` (machine marker log → narrative day-grouped diary, K4) with nashsu/llm_wiki, and adding `created`/`updated` frontmatter timestamps to every generated wiki page. Backend tests green on all touched modules; invariants K4, I1, and I5 verified unchanged.

## Added

- **`created` / `updated` frontmatter on every generated wiki page** (`write_wiki_page`) — `created` is set once and preserved across re-generations (read from the file on disk); `updated` advances on every write. The content hash is computed over the bytes written to disk, eliminating false mismatches in the optimistic-lock (ADR-0035) [K6, I5].

## Changed

- **`schema.md` is now the full llm_wiki contract** — the bootstrap schema (`vault.py`) and the live vault file now describe the complete contract that ingest AI and human curators follow: page-type-to-directory map (entity/concept/source/query/comparison/synthesis + overview/index/log), naming conventions (kebab-case; `author-year-topic` for sources; question-slug for queries), complete frontmatter including `lang` [F3] and optional source fields (`authors`/`year`/`url`/`venue`), Index Format, Log Format, Cross-referencing Rules, and Contradiction Handling [K1].
- **`log.md` is a narrative, day-grouped diary (K4)** — `append_log` now writes `## YYYY-MM-DD` headers (once per day) with one bullet per ingest in the form `- HH:MM:SSZ · <verb> · <type> · [[Title]] — path`. Still append-only and machine-parseable (K4 preserved; AC-K4-1 satisfied). Page deletion via `DELETE /pages` also goes through `append_log`. Existing log history is not rewritten [K4, I5].
- **CI: GitHub Actions pins updated** — eliminates Node 20 deprecation warnings across all workflows (`ci.yml`, `desktop-release.yml`, `release-cut.yml`, `release-notes-sync.yml`): `actions/checkout` v4→v5, `docker/build-push-action` v5→v7, `docker/login-action` v3→v4, `docker/setup-buildx-action` v3→v4, `docker/setup-qemu-action` v3→v4.

## Artifacts

`Synapse_1.3.5_aarch64.dmg` · `Synapse_1.3.5_x64-setup.exe` · `synapse-clipper-1.3.5.zip` · signed `latest.json`

---

**Full changelog**: https://github.com/Emanuele-Chiummo/llm-wiki-synapse/compare/v1.3.4...v1.3.5
